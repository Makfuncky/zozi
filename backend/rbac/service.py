"""rbac/service.py - admin-facing RBAC operations (spec).

Grant/revoke, delegation, maker-checker. Migrated from the legacy RBACService.
"""
class RBACService:
    def grant(self, *args, **kwargs):  # pragma: no cover - stub
        raise NotImplementedError("RBAC grant not yet wired")

    def revoke(self, *args, **kwargs):  # pragma: no cover - stub
        raise NotImplementedError("RBAC revoke not yet wired")
