# Static upward imports (Law 1: arrows point down only).
# Baseline frozen by the architecture gate; the count must only DECREASE.
# Regenerate with tests/_gen_import_laws_baseline.py after an intentional move.

