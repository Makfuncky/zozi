{
  "1": {
    "desc": "Dependencies flow modules → domains → infrastructure → kernel. Reverse imports FORBIDDEN.",
    "fix": "Remove reverse imports. Infrastructure/kernel must not import from domains/modules/rbac/providers."
  },
  "2": {
    "desc": "Router = auth context + require_feature + ONE service call + serialization. No DB writes, no business logic.",
    "fix": "Extract business logic to domain services. Router should only authenticate, gate, parse, call one service, return."
  },
  "3": {
    "desc": "Cross-domain WRITES via events.py/subscribers.py. Cross-domain READS via ports.py only.",
    "fix": "Replace cross-domain imports with ports.py for reads, events.py for writes."
  },
  "4": {
    "desc": "Permission atoms defined once in domains/*/features.py. Aggregated by rbac/catalog.py.",
    "fix": "Create features.py in each domain with FEATURES dict. Register in rbac/catalog.py."
  },
  "5": {
    "desc": "Every data access scoped by country_code via RLS. Independent of feature check.",
    "fix": "Call instrument_rls(engine) in main.py. Ensure CountryContextMiddleware sets RLS context."
  },
  "6": {
    "desc": "Every table in a domain Postgres schema. Alembic is the only schema source.",
    "fix": "Add __table_args__ = {'schema': '<domain>'} to all models. Use keyset pagination instead of OFFSET."
  },
  "7": {
    "desc": "DOMAIN_ALLOWLIST.yaml tracks temporary cross-domain imports. May only shrink.",
    "fix": "Create DOMAIN_ALLOWLIST.yaml. Track all temporary cross-domain imports with expiration dates."
  },
  "8": {
    "desc": "modules/{m}/routers/{d}.py — one file per domain per module (15 per module).",
    "fix": "Create router files for all 16 domains in each module's routers/ directory."
  },
  "9": {
    "desc": "All tools code in providers/ai, providers/image. Tools is an external concern.",
    "fix": "Move domains/media/ to providers/media/, providers/image/, providers/bg_removal/."
  },
  "10": {
    "desc": "kernel/ contains ONLY pure business primitives. No imports from domains/modules/rbac.",
    "fix": "Remove domain/module/rbac/provider imports from kernel. Keep only money, currency, numbering, country, period."
  },
  "11": {
    "desc": "A provider wraps exactly one external SDK. No business logic, no domain imports.",
    "fix": "Ensure each provider wraps one SDK. Remove business logic and domain imports from providers."
  },
  "12": {
    "desc": "Fixed set of 15 domains: accounts, analytics, audit, catalog, comms, country, customers, finance, governance, hr, logistics, orders, promotions, security, suppliers.",
    "fix": "Create missing domains with full structure: services/, models/, schemas/, events.py, subscribers.py, ports.py, features.py."
  },
  "13": {
    "desc": "Fixed set of 5 modules: admin, customer, employee, logistics, supplier.",
    "fix": "Review module structure. Expected 5 modules each with auth/, routers/, serializers/."
  },
  "14": {
    "desc": "All business logic in domain services/services/. No logic in routers or modules.",
    "fix": "Move business logic from routers/modules to appropriate domain services/."
  },
  "15": {
    "desc": "Every HTTP endpoint in a module router file named after the domain.",
    "fix": "Move API endpoints to modules/{module}/routers/{domain}.py files."
  },
  "16": {
    "desc": "Every third-party integration under providers/{category}/.",
    "fix": "Move SDK wrappers to appropriate providers/ subdirectory."
  },
  "17": {
    "desc": "events.py (writes) and ports.py (reads) are the ONLY sanctioned cross-domain channels.",
    "fix": "Create events.py and ports.py in each domain. Remove direct cross-domain imports."
  },
  "18": {
    "desc": "utils/, routers/, controllers/, services/, models/, db/ FORBIDDEN at backend/ root.",
    "fix": "Move contents to appropriate locations: infrastructure/utils/, modules/*/routers/, domains/*/services/."
  },
  "19": {
    "desc": "Monetary values MUST use Decimal or Numeric. Float FORBIDDEN for money.",
    "fix": "Replace Column(Float) with Column(Numeric(precision=19, scale=4)). Use kernel.money.to_decimal."
  },
  "20": {
    "desc": "country_code always String(2) following ISO 3166-1 alpha-2.",
    "fix": "Change country_code to Column(String(2), nullable=False, index=True)."
  },
  "21": {
    "desc": "created_at/updated_at use server_default=func.now() (DB-side), not Python-side.",
    "fix": "Add server_default=func.now() to created_at/updated_at columns."
  },
  "22": {
    "desc": "Every ForeignKey MUST declare explicit ondelete behavior.",
    "fix": "Add ondelete='CASCADE', 'SET NULL', or 'RESTRICT' to all ForeignKey columns."
  },
  "23": {
    "desc": "Every model MUST include created_at, updated_at, country_code, is_deleted.",
    "fix": "Add TimestampMixin and SoftDeleteMixin to all models."
  },
  "24": {
    "desc": "core, platform, identity are FORBIDDEN as Postgres schema names.",
    "fix": "Change __table_args__ to use domain-specific schema name."
  },
  "25": {
    "desc": "All files to correct domains before reorganization. Never replace working code with stubs.",
    "fix": "Shift files to correct domains first. Use existing code (even if broken) and improve it."
  },
  "26": {
    "desc": "Temporary re-exports in infrastructure/utils/ for relocated files.",
    "fix": "Add backward-compat re-exports when moving files between domains."
  },
  "27": {
    "desc": "Root-level fix_*.py, debug_*.py should be removed after use.",
    "fix": "Delete temporary fix_*.py and debug_*.py scripts from root."
  },
  "28": {
    "desc": "_auto_stubs.py are migration scaffolding, not architecture components.",
    "fix": "Remove _auto_stubs.py files. They are temporary scaffolding."
  },
  "29": {
    "desc": "Service Registry and auto_wire.py were removed from architecture.",
    "fix": "Remove registry.py, auto_wire.py, service_index.json, migrate_imports.py."
  },
  "30": {
    "desc": "Domains handle missing SDKs via HAS_<SDK> flags. Never crash.",
    "fix": "Add HAS_<SDK> boolean flags to all providers. Check flags before calling SDK."
  },
  "31": {
    "desc": "Providers MUST NOT import from domains, modules, rbac, jobs, middleware.",
    "fix": "Remove domain/module/rbac/job/middleware imports from providers."
  },
  "32": {
    "desc": "JWT keys, API keys, passwords from env vars or secrets manager only.",
    "fix": "Move all secrets to environment variables or secrets manager. Never hardcode."
  },
  "33": {
    "desc": "All JWT decoders MUST verify the type claim (access vs refresh).",
    "fix": "Add type claim verification to all JWT decode functions."
  },
  "34": {
    "desc": "Use SQLAlchemy text() with bound parameters or ORM. No f-string interpolation.",
    "fix": "Replace f-string SQL with parameterized queries using text() and bind parameters."
  },
  "35": {
    "desc": "CSRF middleware MUST be active in all environments.",
    "fix": "Enable CSRF middleware in all environments."
  },
  "36": {
    "desc": "CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy in production.",
    "fix": "Add SecurityHeadersMiddleware with all required headers."
  },
  "37": {
    "desc": "Redis unreachable = deny requests (not allow all).",
    "fix": "Configure rate limiter to fail closed when Redis is unreachable."
  },
  "38": {
    "desc": "Passwords >72 bytes rejected with error, never truncated.",
    "fix": "Add explicit check: if len(password) > 72: raise ValueError."
  },
  "39": {
    "desc": "Auth logic in exactly one canonical location.",
    "fix": "Consolidate auth logic into modules/{m}/auth/ and rbac/dependencies.py only."
  },
  "40": {
    "desc": "Origin header validated against allowlist before reflection.",
    "fix": "Validate Origin header against allowlist. Don't reflect arbitrary origins."
  },
  "41": {
    "desc": "WebSocket connections MUST verify JWT type claim equals access.",
    "fix": "Add JWT type claim verification to WebSocket authentication."
  },
  "42": {
    "desc": "All public endpoints use Pydantic schemas. No raw dicts in router signatures.",
    "fix": "Add Pydantic request/response schemas to all public endpoints."
  },
  "43": {
    "desc": "Auth failures, 403s, rate-limit triggers logged at WARNING+.",
    "fix": "Add logging for all security events at WARNING level or above."
  },
  "44": {
    "desc": "All deps scanned for CVEs in CI. High/critical block deployment.",
    "fix": "Add dependency scanning (safety, pip-audit, or similar) to CI pipeline."
  },
  "45": {
    "desc": "All relationships declare lazy=selectin or joined. Default lazy=select FORBIDDEN.",
    "fix": "Change lazy='select' to lazy='selectin' or lazy='joined' in all relationships."
  },
  "46": {
    "desc": "Application queries MUST select explicit columns.",
    "fix": "Replace SELECT * with explicit column selection in all queries."
  },
  "47": {
    "desc": "pool_size ≥ 10, max_overflow ≥ 20. Environment-configurable.",
    "fix": "Configure SQLAlchemy engine with pool_size=10, max_overflow=20 minimum."
  },
  "48": {
    "desc": "Replica connections use independent pool settings. Read-heavy uses get_read_db().",
    "fix": "Use get_read_db() for read-heavy operations. Configure separate pool for replicas."
  },
  "49": {
    "desc": "Alembic history MUST remain linear. Resolve divergent heads immediately.",
    "fix": "Run alembic merge heads to resolve divergent history."
  },
  "50": {
    "desc": "All writes use explicit transaction management. Autocommit FORBIDDEN.",
    "fix": "Use explicit db.commit() or context managers for all write operations."
  },
  "51": {
    "desc": "Each table defined in exactly one domain. Duplicate __tablename__ FORBIDDEN.",
    "fix": "Remove duplicate table definitions. Each table belongs to exactly one domain."
  },
  "52": {
    "desc": "All FK columns have explicit ForeignKey with ondelete.",
    "fix": "Add ForeignKey constraint with ondelete to all foreign key columns."
  },
  "53": {
    "desc": "All FK columns have explicit index. PostgreSQL does NOT auto-index FKs.",
    "fix": "Add index=True to all ForeignKey columns or create explicit indexes."
  },
  "54": {
    "desc": "All user-facing tables use is_deleted (boolean, default false).",
    "fix": "Add is_deleted = Column(Boolean, default=False, nullable=False) to user-facing tables."
  },
  "55": {
    "desc": "Every model declares __table_args__ = {schema: <domain>}.",
    "fix": "Add __table_args__ = {'schema': '<domain>'} to all models."
  },
  "56": {
    "desc": "core, platform, identity FORBIDDEN as schema names.",
    "fix": "Rename schemas to use domain-specific names."
  },
  "57": {
    "desc": "Destructive migrations include backward-compatible strategy (expand-contract).",
    "fix": "Use multi-step migrations: add new → backfill → switch reads → drop old."
  },
  "58": {
    "desc": "print() FORBIDDEN in production code. Use structlog logger.",
    "fix": "Replace all print() calls with logger.info/debug/warning/error."
  },
  "59": {
    "desc": "All except blocks MUST log at minimum DEBUG level.",
    "fix": "Add logging to all exception handlers. Never use bare except: pass."
  },
  "60": {
    "desc": "Async functions MUST NOT call blocking I/O. Use asyncio.sleep, httpx.",
    "fix": "Replace time.sleep with asyncio.sleep, requests with httpx in async functions."
  },
  "61": {
    "desc": "All in-memory caches have max size and/or TTL. Unbounded dict FORBIDDEN.",
    "fix": "Use functools.lru_cache(maxsize=) or cachetools.TTLCache for all caches."
  },
  "62": {
    "desc": "TODO/FIXME must include ticket reference and expiration date.",
    "fix": "Add ticket reference and expiration date to all TODO/FIXME comments."
  },
  "63": {
    "desc": "All public function signatures MUST have type hints.",
    "fix": "Add type hints to all public function parameters and return values."
  },
  "64": {
    "desc": "Functions SHOULD NOT exceed 50 lines (excl. docstrings/blanks).",
    "fix": "Refactor long functions into smaller, focused functions."
  },
  "65": {
    "desc": "Maximum 4 levels of indentation per function.",
    "fix": "Refactor deeply nested code using early returns or extraction."
  },
  "66": {
    "desc": "Numeric constants MUST be named constants or config values.",
    "fix": "Replace magic numbers with named constants at module level."
  },
  "67": {
    "desc": "Duplicate code blocks (>5 lines) MUST be extracted into shared functions.",
    "fix": "Extract duplicate code into shared utility functions."
  },
  "68": {
    "desc": "All service functions use consistent error pattern (exceptions or Result).",
    "fix": "Standardize error handling across all service functions."
  },
  "69": {
    "desc": "Every domain MUST have at least one smoke test.",
    "fix": "Create smoke tests for all domains in tests/domains/."
  },
  "70": {
    "desc": "Every statically checkable law MUST have a corresponding test.",
    "fix": "Add architecture tests in tests/architecture/ for all checkable laws."
  },
  "71": {
    "desc": "Collection-time failures MUST block CI. Fix within 24h.",
    "fix": "Configure CI to fail on test collection errors. Fix broken tests immediately."
  },
  "72": {
    "desc": "Cross-domain flows MUST have integration tests.",
    "fix": "Create integration tests for all cross-domain flows."
  },
  "73": {
    "desc": "Critical paths MUST have perf regression tests. Fail on >20% latency.",
    "fix": "Add performance regression tests for critical paths."
  },
  "74": {
    "desc": "All tests use transaction-rolled-back sessions. No data leaks.",
    "fix": "Ensure all tests use db_session fixture with rollback."
  },
  "75": {
    "desc": "External failures MUST degrade gracefully AND log WARNING.",
    "fix": "Add try/except with logging for all external calls."
  },
  "76": {
    "desc": "Sessions managed via FastAPI dependency injection only.",
    "fix": "Use Depends(get_db) for all session management. Never create sessions manually."
  },
  "77": {
    "desc": "Async resources use asyncio.Lock for init and disposal.",
    "fix": "Add asyncio.Lock to async resource initialization and cleanup."
  },
  "78": {
    "desc": "Pipeline order FIXED: Foundation → Auth → Rate → Webhook → Geo → Security → Observe → Compliance.",
    "fix": "Verify middleware/orchestrator.py has correct pipeline order."
  },
  "79": {
    "desc": "Global exception handler catches all uncaught exceptions. Returns structured response.",
    "fix": "Add @app.exception_handler(Exception) to return RFC 7807 responses."
  },
  "80": {
    "desc": "Shutdown disposes DB engine, Redis, workers via lifespan.py.",
    "fix": "Add lifespan handler that disposes all resources on shutdown."
  },
  "81": {
    "desc": "/health, /health/deps, /health/ready verify all critical dependencies.",
    "fix": "Create health check endpoints that verify all dependencies."
  },
  "82": {
    "desc": "Config fallbacks MUST NOT contain real credentials. Empty/null default.",
    "fix": "Remove default credentials from config. Use None/empty defaults."
  },
  "83": {
    "desc": "Required env vars validated at startup. Missing = immediate failure.",
    "fix": "Add startup validation for SECRET_KEY, DATABASE_URL, REDIS_URL."
  },
  "84": {
    "desc": "Flags use pydantic-settings. Raw os.getenv() FORBIDDEN.",
    "fix": "Replace os.getenv() with pydantic-settings BaseSettings class."
  },
  "85": {
    "desc": "Secrets rotatable without deployment via env var updates.",
    "fix": "Ensure all secrets are loaded from env vars, not hardcoded."
  },
  "86": {
    "desc": "Prod, staging, dev have separate config profiles. No inheritance.",
    "fix": "Create separate config profiles for each environment."
  },
  "87": {
    "desc": "All non-public endpoints MUST use get_current_user or equivalent.",
    "fix": "Add Depends(get_current_user) to all protected endpoints."
  },
  "88": {
    "desc": "All non-public endpoints MUST use require_feature() or require_module().",
    "fix": "Add require_feature() or require_module() to all protected endpoints."
  },
  "89": {
    "desc": "Routers return serialized responses (Pydantic/dicts), never raw ORM models.",
    "fix": "Convert ORM models to Pydantic schemas before returning from routers."
  },
  "90": {
    "desc": "Routers contain ONLY: auth, gate, parsing, ONE service call, serialization.",
    "fix": "Remove all business logic from routers. Call only one service per endpoint."
  },
  "91": {
    "desc": "All endpoints MUST have OpenAPI-compatible docstrings.",
    "fix": "Add docstrings to all endpoint functions for OpenAPI documentation."
  },
  "92": {
    "desc": "All logs use structlog with context (user_id, request_id, domain, action).",
    "fix": "Add structlog context to all log messages."
  },
  "93": {
    "desc": "All requests carry request_id propagated through service calls.",
    "fix": "Add request_id to all log context and propagate through service calls."
  },
  "94": {
    "desc": "Critical paths emit Prometheus metrics (latency, error rate, throughput).",
    "fix": "Add Prometheus instrumentation to all critical paths."
  },
  "95": {
    "desc": "Unhandled exceptions reported to Sentry with fallback to logger.",
    "fix": "Configure Sentry SDK with fallback to structlog."
  },
  "96": {
    "desc": "State-changing operations write to domains/audit/.",
    "fix": "Add audit logging to all state-changing operations."
  },
  "97": {
    "desc": "Imports MUST follow modules → domains → infrastructure → kernel.",
    "fix": "Fix all reverse imports. Arrows must point down only."
  },
  "98": {
    "desc": "Circular chains between any two packages FORBIDDEN.",
    "fix": "Break circular imports using TYPE_CHECKING or extraction."
  },
  "99": {
    "desc": "Modules don't import infrastructure directly. Domains don't import modules.",
    "fix": "Remove cross-layer imports. Use proper dependency direction."
  },
  "100": {
    "desc": "Providers don't import from domains/modules/rbac/jobs/middleware.",
    "fix": "Remove all domain/module/rbac/job/middleware imports from providers."
  },
  "101": {
    "desc": "kernel/ doesn't import from domains/modules/rbac/providers/jobs/middleware.",
    "fix": "Remove all upper-layer imports from kernel."
  },
  "102": {
    "desc": "infrastructure/ doesn't import from domains/modules/rbac/providers.",
    "fix": "Remove all upper-layer imports from infrastructure."
  },
  "103": {
    "desc": "Jobs import from domains/ + infrastructure/ only.",
    "fix": "Remove module/rbac/middleware imports from jobs."
  },
  "104": {
    "desc": "Middleware imports from infrastructure/ + rbac/ only.",
    "fix": "Remove domain/module imports from middleware."
  },
  "105": {
    "desc": "@zozi/shared doesn't import from web_app or mobile_app.",
    "fix": "Remove app imports from shared package."
  },
  "106": {
    "desc": "Frontend communicates via API proxy only. No direct backend imports.",
    "fix": "Use API proxy for all backend communication."
  },
  "107": {
    "desc": "Production uses PostgreSQL 15. SQLite for dev/test only.",
    "fix": "Configure PostgreSQL for production, SQLite for development."
  },
  "108": {
    "desc": "SQLite for zero-config startup. File in .gitignore.",
    "fix": "Add SQLite file to .gitignore."
  },
  "109": {
    "desc": "Redis for sessions, catalog cache, rate limiting, blacklist, pub/sub.",
    "fix": "Configure Redis for all caching and session storage."
  },
  "110": {
    "desc": "Sessions→DB fallback, caching pass-through, rate limit fails closed.",
    "fix": "Implement Redis failure handling with graceful degradation."
  },
  "111": {
    "desc": "Web uses Next.js App Router, not Pages Router.",
    "fix": "Ensure Next.js uses App Router (src/app directory)."
  },
  "112": {
    "desc": "Data-fetching = Server Components. Client = interactivity/hooks.",
    "fix": "Use Server Components for data fetching, Client Components for interactivity."
  },
  "113": {
    "desc": "Mobile uses Expo Router with file-based routing.",
    "fix": "Configure Expo Router for mobile navigation."
  },
  "114": {
    "desc": "WebSocket only for real-time (notifications, chat, tracking).",
    "fix": "Use WebSocket only for real-time features. Use REST for everything else."
  },
  "115": {
    "desc": "CPU-bound and async jobs use Celery with Redis broker.",
    "fix": "Configure Celery for background job processing."
  },
  "116": {
    "desc": "Transactional email via providers/comms/email.py. Async sending.",
    "fix": "Use async email sending via SMTP provider."
  },
  "117": {
    "desc": "SMS and WhatsApp via Twilio. Async sending.",
    "fix": "Use async SMS sending via Twilio provider."
  },
  "118": {
    "desc": "Stripe, Tap, PayPal, PayTabs, Thawani. Each implements BasePaymentGateway.",
    "fix": "Ensure all payment providers implement BasePaymentGateway."
  },
  "119": {
    "desc": "Ollama, OpenAI, HuggingFace via providers/ai/. CPU via async_workers.",
    "fix": "Configure AI providers with async workers for CPU-bound tasks."
  },
  "120": {
    "desc": "S3 (prod) or local (dev). Media blobs never in PostgreSQL.",
    "fix": "Use S3 for production storage, local for development."
  },
  "121": {
    "desc": "Pillow, rembg, OpenCV. Heavy processing via async_workers.",
    "fix": "Use async workers for image processing tasks."
  },
  "122": {
    "desc": "Leaflet + react-leaflet. Geocoding via providers/geography/.",
    "fix": "Configure Leaflet maps with geography provider."
  },
  "123": {
    "desc": "Each provider wraps exactly one external SDK or service.",
    "fix": "Split multi-SDK providers into separate modules."
  },
  "124": {
    "desc": "Every provider exposes HAS_<SDK> boolean flags.",
    "fix": "Add HAS_<SDK> = True/False to all provider modules."
  },
  "125": {
    "desc": "When HAS_<SDK> = False, return defaults. Never crash.",
    "fix": "Add graceful degradation when SDK is unavailable."
  },
  "126": {
    "desc": "Providers contain ONLY SDK wrapping: auth, formatting, parsing, error mapping.",
    "fix": "Remove business logic from providers."
  },
  "127": {
    "desc": "API keys, endpoints, timeouts in providers/config.py.",
    "fix": "Move provider configuration to providers/config.py."
  },
  "128": {
    "desc": "CPU-bound provider work via providers/async_workers.",
    "fix": "Route CPU-bound work through async_workers."
  },
  "129": {
    "desc": "All providers expose health_check() via BaseProvider.",
    "fix": "Add health_check() method to all providers."
  },
  "130": {
    "desc": "SDK errors mapped to domain exceptions. Raw SDK errors don't leak.",
    "fix": "Map all SDK errors to domain-specific exceptions."
  },
  "131": {
    "desc": "Provider tests mock the external SDK. No real API calls.",
    "fix": "Mock external SDKs in all provider tests."
  },
  "132": {
    "desc": "modules/{name}/ with auth/, routers/, serializers/.",
    "fix": "Ensure each module has auth/, routers/, serializers/ directories."
  },
  "133": {
    "desc": "Each module has its own auth dependency in auth/dependencies.py.",
    "fix": "Create auth/dependencies.py in each module."
  },
  "134": {
    "desc": "modules/{module}/routers/{domain}.py — one file per domain.",
    "fix": "Name router files after domains, not actions."
  },
  "135": {
    "desc": "All router files listed in routers/__init__.py.",
    "fix": "Register all routers in __init__.py."
  },
  "136": {
    "desc": "public_routers (no auth) vs routers (auth required).",
    "fix": "Separate public and protected routers."
  },
  "137": {
    "desc": "Response serializers in modules/{module}/serializers/.",
    "fix": "Move serializers to module's serializers/ directory."
  },
  "138": {
    "desc": "admin, customer, employee, logistics, supplier. New requires review.",
    "fix": "Review any new modules against the fixed set."
  },
  "139": {
    "desc": "/admin/*, /customer/*, /employee/*, /logistics-partner/*, /supplier/*.",
    "fix": "Configure correct route prefixes for each module."
  },
  "140": {
    "desc": "database/, redis/, storage/, messaging/, observability/, security/, utils/.",
    "fix": "Ensure all 7 infrastructure subpackages exist."
  },
  "141": {
    "desc": "Base, get_db/get_read_db, sessions, RLS, transactions, seeds.",
    "fix": "Create all database infrastructure files."
  },
  "142": {
    "desc": "Client singleton, cache abstraction, blacklist, pub/sub.",
    "fix": "Create Redis infrastructure with singleton client."
  },
  "143": {
    "desc": "Abstraction interface + backup utilities.",
    "fix": "Create storage abstraction interface."
  },
  "144": {
    "desc": "WS manager, realtime, email wrappers, event bus.",
    "fix": "Create messaging infrastructure."
  },
  "145": {
    "desc": "OTEL, Prometheus, structlog, Sentry, circuit breaker.",
    "fix": "Create observability infrastructure."
  },
  "146": {
    "desc": "JWT, bcrypt, KMS encryption, rate limiting, CSRF, country access.",
    "fix": "Create security infrastructure."
  },
  "147": {
    "desc": "Pure technical helpers: pagination, datetime, config, caching, HTTP.",
    "fix": "Create utils infrastructure with pure helpers."
  },
  "148": {
    "desc": "infrastructure.database.base.Base is THE base. Others FORBIDDEN.",
    "fix": "Use only infrastructure.database.base.Base for all models."
  },
  "149": {
    "desc": "Sessions via FastAPI Depends(get_db) only.",
    "fix": "Remove manual session creation. Use Depends(get_db)."
  },
  "150": {
    "desc": "services/, models/, schemas/, events.py, subscribers.py, ports.py, features.py, read_models/, policies/.",
    "fix": "Create all required domain files and directories."
  },
  "151": {
    "desc": "Services take primitives, own DB access and transactions.",
    "fix": "Ensure services own their DB access and transactions."
  },
  "152": {
    "desc": "__tablename__ + __table_args__ = {schema: <domain>}. Canonical Base.",
    "fix": "Add __tablename__ and __table_args__ to all models."
  },
  "153": {
    "desc": "Pydantic models for validation. Used by routers (in) and serializers (out).",
    "fix": "Create Pydantic schemas for all domain models."
  },
  "154": {
    "desc": "Named {domain}.{entity}.{action}. Minimal data (IDs, not objects).",
    "fix": "Use consistent event naming with minimal data."
  },
  "155": {
    "desc": "Sanctioned cross-domain READ path. Take db + primitives, return results.",
    "fix": "Create ports.py with read functions for cross-domain access."
  },
  "156": {
    "desc": "Handle events from other domains. Resolve context, call services.",
    "fix": "Create subscribers.py to handle cross-domain events."
  },
  "157": {
    "desc": "FEATURES = {key: description}. Consistent format across domains.",
    "fix": "Define FEATURES dict in each domain's features.py."
  },
  "158": {
    "desc": "CQRS-lite projections for this domain's dashboards.",
    "fix": "Create read_models/ for domain-specific projections."
  },
  "159": {
    "desc": "Authorization policies. Enforced by require_feature() and service checks.",
    "fix": "Create policies/ for domain authorization rules."
  },
  "160": {
    "desc": "New domains require architecture review.",
    "fix": "Review any new domains against the fixed 15-domain set."
  },
  "161": {
    "desc": "Aggregates all features.py via package scan.",
    "fix": "Implement rbac/catalog.py to scan all features.py files."
  },
  "162": {
    "desc": "(module, role) → feature sets. Static code, not DB-driven.",
    "fix": "Define ROLE_FEATURES mapping in rbac/roles.py."
  },
  "163": {
    "desc": "actor × role × country → effective set. Redis-cached.",
    "fix": "Implement rbac/resolution.py with Redis caching."
  },
  "164": {
    "desc": "require_feature(...) and require_module(...) FastAPI dependencies.",
    "fix": "Create rbac/dependencies.py with gate functions."
  },
  "165": {
    "desc": "Grant/revoke, delegation, maker-checker. All changes audited.",
    "fix": "Create rbac/service.py with audit logging."
  },
  "166": {
    "desc": "Categories, permissions, assignments, overrides, audit log.",
    "fix": "Create rbac/models.py with permission models."
  },
  "167": {
    "desc": "permissions.ts GENERATED from GET /rbac/catalog.",
    "fix": "Add generation marker to permissions.ts."
  },
  "168": {
    "desc": "web_app/ (Next.js), mobile_app/ (Expo), shared/ (cross-platform).",
    "fix": "Ensure monorepo structure is correct."
  },
  "169": {
    "desc": "16.3.1+ with App Router.",
    "fix": "Update Next.js to 16.3.1+ with App Router."
  },
  "170": {
    "desc": "strict: true. any requires justification.",
    "fix": "Enable strict mode in tsconfig.json."
  },
  "171": {
    "desc": "Zustand (global), React (local), Query/SWR (server).",
    "fix": "Use appropriate state management for each concern."
  },
  "172": {
    "desc": "Server Components fetch directly. Client Components use API client.",
    "fix": "Use Server Components for data fetching."
  },
  "173": {
    "desc": "Next.js rewrites /api/*, /admin/*, etc. to NEXT_PUBLIC_API_URL.",
    "fix": "Configure API proxy rewrites in next.config."
  },
  "174": {
    "desc": "Tailwind CSS + CVA + tailwind-merge + clsx.",
    "fix": "Configure styling libraries correctly."
  },
  "175": {
    "desc": "React Hook Form + Zod.",
    "fix": "Use React Hook Form with Zod validation."
  },
  "176": {
    "desc": "API errors → toasts/boundaries. Unhandled → console (dev) / Sentry (prod).",
    "fix": "Implement error boundaries and toast notifications."
  },
  "177": {
    "desc": "Organized by actor: (customer), auth/, admin/*, supplier/*, etc.",
    "fix": "Organize route groups by actor."
  },
  "178": {
    "desc": "page.tsx, layout.tsx, loading.tsx, error.tsx per route.",
    "fix": "Create all required route files."
  },
  "179": {
    "desc": "ui/ (design system), admin/, auth/, chat/, comms/, country/, etc.",
    "fix": "Organize components by area."
  },
  "180": {
    "desc": "useXxx prefix. No JSX in hooks.",
    "fix": "Rename hooks to use prefix. Move JSX to components."
  },
  "181": {
    "desc": "api/ (client, auth, country, errors), rbac.ts.",
    "fix": "Organize lib/ correctly."
  },
  "182": {
    "desc": "localizationService, crossBorderService, addressFormatService.",
    "fix": "Create client-side services."
  },
  "183": {
    "desc": "Design tokens, Tailwind config, global CSS.",
    "fix": "Configure theme and styling."
  },
  "184": {
    "desc": "From @zozi/shared + local definitions.",
    "fix": "Import types from @zozi/shared."
  },
  "185": {
    "desc": "Pure utility functions.",
    "fix": "Keep utils as pure functions only."
  },
  "186": {
    "desc": "next build passes with no errors.",
    "fix": "Fix all build errors."
  },
  "187": {
    "desc": "Expo Router with file-based routing.",
    "fix": "Configure Expo Router."
  },
  "188": {
    "desc": "(auth), (tabs), admin/, supplier/, logistics/, employee/.",
    "fix": "Organize mobile route groups."
  },
  "189": {
    "desc": "components/ui/ design system. Shared from @zozi/shared.",
    "fix": "Create mobile design system."
  },
  "190": {
    "desc": "api, stores, authPrompt, countryContext, geo, payment, secureStorage.",
    "fix": "Organize mobile lib correctly."
  },
  "191": {
    "desc": ".native.ts (RN) / .ts (web) suffixes.",
    "fix": "Use platform-specific file suffixes."
  },
  "192": {
    "desc": "Zustand (same stores as web).",
    "fix": "Use Zustand for mobile state."
  },
  "193": {
    "desc": "expo-secure-storage for secrets. AsyncStorage for non-sensitive.",
    "fix": "Use secure storage for secrets."
  },
  "194": {
    "desc": "EAS Build. Expo Go for dev. App stores for production.",
    "fix": "Configure EAS Build."
  },
  "195": {
    "desc": "api-core, money, i18n, domain helpers, statusColors, requestCache, etc.",
    "fix": "Create shared package structure."
  },
  "196": {
    "desc": "MUST NOT import from web_app/ or mobile_app/.",
    "fix": "Remove app imports from shared."
  },
  "197": {
    "desc": "permissions.ts GENERATED from backend /rbac/catalog.",
    "fix": "Add generation marker to permissions.ts."
  },
  "198": {
    "desc": "Platform-agnostic. .native.ts for platform adaptations.",
    "fix": "Keep shared types platform-agnostic."
  },
  "199": {
    "desc": "apiFetch base client with auth, errors, retry.",
    "fix": "Create api-core with auth and retry."
  },
  "200": {
    "desc": "Intl.NumberFormat with locale support.",
    "fix": "Use Intl.NumberFormat for money formatting."
  },
  "201": {
    "desc": ".env.example → .env → backend/.env → frontend/.env.local.",
    "fix": "Create proper env file hierarchy."
  },
  "202": {
    "desc": "SECRET_KEY, DATABASE_URL, REDIS_URL MUST be set in prod.",
    "fix": "Add startup validation for required env vars."
  },
  "203": {
    "desc": "pydantic-settings. No raw os.getenv().",
    "fix": "Replace os.getenv() with pydantic-settings."
  },
  "204": {
    "desc": "AWS Secrets Manager / HashiCorp Vault in production.",
    "fix": "Configure secrets manager for production."
  },
  "205": {
    "desc": "development, test, staging, production.",
    "fix": "Use APP_ENV for environment detection."
  },
  "206": {
    "desc": "Comma-separated list. No wildcard in production.",
    "fix": "Configure CORS allowlist without wildcards."
  },
  "207": {
    "desc": "pytest + pytest-asyncio. Files in tests/domains/ and tests/architecture/.",
    "fix": "Configure pytest with asyncio support."
  },
  "208": {
    "desc": "db_session, client/admin_client/supplier_client/customer_client, JWT tokens.",
    "fix": "Create all test fixtures."
  },
  "209": {
    "desc": "admin@zozi.com, supplier@zozi.com, customer@zozi.com.",
    "fix": "Seed demo users for testing."
  },
  "210": {
    "desc": "APP_ENV=test. CSRF/rate disabled. SQLite.",
    "fix": "Configure test environment."
  },
  "211": {
    "desc": "Jest + RTL. E2E: Playwright.",
    "fix": "Configure frontend testing."
  },
  "212": {
    "desc": "test_import_laws.py (Laws 1,4,W1,W2), test_feature_catalog.py (Law 4).",
    "fix": "Create architecture test files."
  },
  "213": {
    "desc": "Per-domain smoke tests. Per-law tests. CI-enforced.",
    "fix": "Add smoke tests for all domains."
  },
  "214": {
    "desc": "Transaction-rolled-back. No leaks. Independent.",
    "fix": "Ensure test isolation with rollback."
  },
  "215": {
    "desc": "db, redis, backend, frontend, Celery workers, beat.",
    "fix": "Create docker-compose.yml with all services."
  },
  "216": {
    "desc": "Backend: Railway. Frontend: Vercel. Mobile: EAS Build.",
    "fix": "Configure production deployment targets."
  },
  "217": {
    "desc": "alembic upgrade head automatic before serving traffic.",
    "fix": "Add migration step to deploy script."
  },
  "218": {
    "desc": "/health, /health/deps, /health/ready return 200.",
    "fix": "Create health check endpoints."
  },
  "219": {
    "desc": "MUST support rollback. Migrations backward-compatible.",
    "fix": "Implement rollback strategy."
  },
  "220": {
    "desc": "development → staging → production.",
    "fix": "Configure environment promotion pipeline."
  },
  "221": {
    "desc": "Redis with TTL. Event-driven invalidation.",
    "fix": "Implement Redis caching with TTL."
  },
  "222": {
    "desc": "cursor_paginate_asc. OFFSET FORBIDDEN on hot lists.",
    "fix": "Replace OFFSET with keyset pagination."
  },
  "223": {
    "desc": "pool_size ≥ 10, max_overflow ≥ 20. PgBouncer.",
    "fix": "Configure connection pooling."
  },
  "224": {
    "desc": "Indexes on queried columns. No N+1. No SELECT *.",
    "fix": "Add indexes and optimize queries."
  },
  "225": {
    "desc": "Static assets, images via CDN. Presigned URLs.",
    "fix": "Configure CDN for static assets."
  },
  "226": {
    "desc": "CPU-bound work to Celery. Request handlers never block.",
    "fix": "Route CPU-bound work to Celery."
  },
  "227": {
    "desc": "set_rls_context() sets country_code. All queries filtered.",
    "fix": "Implement RLS enforcement."
  },
  "228": {
    "desc": "is_deleted (boolean, default false). Queries filter by default.",
    "fix": "Add soft delete to all user-facing tables."
  },
  "229": {
    "desc": "created_at/updated_at via TimestampMixin. DB-side defaults.",
    "fix": "Use TimestampMixin for audit columns."
  },
  "230": {
    "desc": "WORM log. Actor, action, entity, timestamp, before/after.",
    "fix": "Implement WORM audit trail."
  },
  "231": {
    "desc": "MAY shard by country_code.",
    "fix": "Consider data residency requirements."
  },
  "232": {
    "desc": "Daily backups. Tested for recovery. PITR. 30-day retention.",
    "fix": "Implement backup and recovery strategy."
  },
  "233": {
    "desc": "GET (list/detail), POST (create), PUT (update), DELETE.",
    "fix": "Follow REST conventions for all endpoints."
  },
  "234": {
    "desc": "URL prefix /api/v1/. Breaking = new version. Old deprecated.",
    "fix": "Add API version prefix."
  },
  "235": {
    "desc": "Request/response JSON. Pydantic validation. Serializer output.",
    "fix": "Use JSON for all API communication."
  },
  "236": {
    "desc": "Problem Details. No stack traces in production.",
    "fix": "Implement RFC 7807 error responses."
  },
  "237": {
    "desc": "items + next_cursor + has_more. No count on hot lists.",
    "fix": "Use cursor-based pagination format."
  },
  "238": {
    "desc": "filter[field]=value, sort=-created_at. Complex: POST body.",
    "fix": "Implement filtering and sorting."
  },
  "239": {
    "desc": "Idempotency-Key header. 24h expiry.",
    "fix": "Add idempotency support."
  },
  "240": {
    "desc": "main, feature/*, fix/*, release/*, hotfix/*.",
    "fix": "Follow Git Flow branching model."
  },
  "241": {
    "desc": "type(scope): description. feat, fix, refactor, docs, test, chore, perf, security.",
    "fix": "Use Conventional Commits."
  },
  "242": {
    "desc": "All via PR. CI + review. No direct main pushes.",
    "fix": "Enforce PR process with CI."
  },
  "243": {
    "desc": "Pre-commit: ruff. Pre-push: architecture tests.",
    "fix": "Configure git hooks."
  },
  "244": {
    "desc": "Agent Manager uses worktrees. Cleaned up after merge.",
    "fix": "Use worktrees for parallel development."
  },
  "245": {
    "desc": "ARCHITECTURE_DIAGRAM.md is authoritative. Lock-step with code.",
    "fix": "Keep architecture docs in sync with code."
  },
  "246": {
    "desc": "AGENTS.md quick reference. Not the full architecture.",
    "fix": "Keep AGENTS.md concise."
  },
  "247": {
    "desc": "FastAPI auto-generates at /docs and /redoc.",
    "fix": "Ensure FastAPI docs are accessible."
  },
  "248": {
    "desc": "docs/runbooks/. Deployment, rollback, incident response.",
    "fix": "Create runbooks for operations."
  },
  "249": {
    "desc": "Docstrings + WHY comments. Outdated removed.",
    "fix": "Add meaningful comments and docstrings."
  },
  "250": {
    "desc": "CHANGELOG.md. Per-release: features, breaking changes, fixes.",
    "fix": "Maintain changelog."
  },
  "251": {
    "desc": "N stateless replicas. Sessions in Redis.",
    "fix": "Design for horizontal scaling."
  },
  "252": {
    "desc": "CPU > 70% scale up. Min 2, max 20.",
    "fix": "Configure auto-scaling policies."
  },
  "253": {
    "desc": "Range partition by created_at (monthly).",
    "fix": "Implement table partitioning."
  },
  "254": {
    "desc": "Commands write. Events update read models.",
    "fix": "Implement CQRS pattern."
  },
  "255": {
    "desc": "Buffer in Redis, flush async.",
    "fix": "Implement write-behind caching."
  },
  "256": {
    "desc": "Per-country limits. 429 on exceed.",
    "fix": "Implement tenant quotas."
  },
  "257": {
    "desc": "Elasticsearch/OpenSearch for catalog.",
    "fix": "Configure full-text search."
  },
  "258": {
    "desc": "Async resize, WebP, metadata strip.",
    "fix": "Implement image processing pipeline."
  },
  "259": {
    "desc": "ETag, Last-Modified, Cache-Control. CDN.",
    "fix": "Add API caching headers."
  },
  "260": {
    "desc": "Transaction pooling. max_client_conn=10000.",
    "fix": "Configure PgBouncer."
  },
  "261": {
    "desc": "Auto-route reads. Fallback if lag > 1s.",
    "fix": "Configure read replicas."
  },
  "262": {
    "desc": "Old data to S3 Glacier.",
    "fix": "Implement data archiving."
  },
  "263": {
    "desc": "Celery queue for bursty writes.",
    "fix": "Use Celery for write buffering."
  },
  "264": {
    "desc": "Minify, compress, hash, CDN.",
    "fix": "Optimize static assets."
  },
  "265": {
    "desc": "Alerts on connections, lag, deadlocks, slow queries.",
    "fix": "Configure DB monitoring."
  },
  "266": {
    "desc": "Every 60s from multiple regions.",
    "fix": "Implement synthetic monitoring."
  },
  "267": {
    "desc": "Max body (10MB), query (100 items), time (30s).",
    "fix": "Add endpoint limits."
  },
  "268": {
    "desc": "Shed non-critical first.",
    "fix": "Implement load shedding."
  },
  "269": {
    "desc": "Right-size, spot instances, coalescing.",
    "fix": "Optimize costs."
  },
  "270": {
    "desc": "Regular failure experiments.",
    "fix": "Implement chaos engineering."
  },
  "271": {
    "desc": "Prompt injection prevention.",
    "fix": "Add AI-agent security measures."
  },
  "272": {
    "desc": "Per-user export limits.",
    "fix": "Implement data exfiltration prevention."
  },
  "273": {
    "desc": "ML input validation before training.",
    "fix": "Validate ML training data."
  },
  "274": {
    "desc": "Signature + anomaly detection.",
    "fix": "Implement adversarial detection."
  },
  "275": {
    "desc": "AES-256. KMS. TDE.",
    "fix": "Implement encryption at rest."
  },
  "276": {
    "desc": "TLS 1.3. Certificate pinning.",
    "fix": "Configure TLS 1.3."
  },
  "277": {
    "desc": "Every 90 days. Zero-downtime.",
    "fix": "Implement key rotation."
  },
  "278": {
    "desc": "Write Once Read Many. Tamper-proof.",
    "fix": "Implement WORM audit."
  },
  "279": {
    "desc": "Device fingerprint. Concurrent limits.",
    "fix": "Implement session binding."
  },
  "280": {
    "desc": "5 fails = lock. 10 = admin.",
    "fix": "Implement brute force protection."
  },
  "281": {
    "desc": "Score + block/CAPTCHA.",
    "fix": "Implement bot detection."
  },
  "282": {
    "desc": "In logs, errors, non-admin responses.",
    "fix": "Implement PII masking."
  },
  "283": {
    "desc": "TOTP for admin/employee.",
    "fix": "Enforce MFA for privileged roles."
  },
  "284": {
    "desc": "mTLS service-to-service.",
    "fix": "Implement zero-trust networking."
  },
  "285": {
    "desc": "Strict + nonces.",
    "fix": "Configure CSP with nonces."
  },
  "286": {
    "desc": "Integrity hashes.",
    "fix": "Implement SRI."
  },
  "287": {
    "desc": "Permissions-Policy, COOP, CORP.",
    "fix": "Add all security headers."
  },
  "288": {
    "desc": "SECURITY.md. 24h SLA for critical.",
    "fix": "Create security policy."
  },
  "289": {
    "desc": "Annual third-party. OWASP + logic.",
    "fix": "Schedule pen testing."
  },
  "290": {
    "desc": "Exact + hashes.",
    "fix": "Pin dependencies with hashes."
  },
  "291": {
    "desc": "For every release.",
    "fix": "Generate SBOM."
  },
  "292": {
    "desc": "CI-enforced.",
    "fix": "Add license compliance to CI."
  },
  "293": {
    "desc": "Auto-isolate, revoke, capture.",
    "fix": "Implement incident automation."
  },
  "294": {
    "desc": "Annual. OWASP + social engineering.",
    "fix": "Provide security training."
  },
  "295": {
    "desc": "Image scanning + signing.",
    "fix": "Implement supply chain security."
  },
  "296": {
    "desc": "All external calls wrapped.",
    "fix": "Implement circuit breaker pattern."
  },
  "297": {
    "desc": "1-2-4-8s. Jitter. Max 5.",
    "fix": "Implement retry with backoff."
  },
  "298": {
    "desc": "Failed events to DLQ. Replayable.",
    "fix": "Implement dead letter queue."
  },
  "299": {
    "desc": "Per-feature in /health/deps.",
    "fix": "Add feature-level health checks."
  },
  "300": {
    "desc": "Each feature defines degradation.",
    "fix": "Implement per-feature fallbacks."
  },
  "301": {
    "desc": "Exhaustion = freeze.",
    "fix": "Implement error budget."
  },
  "302": {
    "desc": "PagerDuty/Opsgenie. 5min SLA.",
    "fix": "Configure on-call rotation."
  },
  "303": {
    "desc": "Per-alert. Tested quarterly.",
    "fix": "Create runbooks for all alerts."
  },
  "304": {
    "desc": "RPO=5min, RTO=1hr. Multi-region.",
    "fix": "Implement disaster recovery."
  },
  "305": {
    "desc": "30s promotion.",
    "fix": "Configure DB failover."
  },
  "306": {
    "desc": "≥2 regions. Cross-region replication.",
    "fix": "Deploy to multiple regions."
  },
  "307": {
    "desc": "Daily restore test.",
    "fix": "Verify backups daily."
  },
  "308": {
    "desc": "IaC daily checks.",
    "fix": "Implement drift detection."
  },
  "309": {
    "desc": "External status pages. Auto-fallback.",
    "fix": "Monitor external dependencies."
  },
  "310": {
    "desc": "Blameless. Action items tracked.",
    "fix": "Implement post-incident reviews."
  },
  "311": {
    "desc": "Gradual rollout. Instant rollback.",
    "fix": "Implement feature flags."
  },
  "312": {
    "desc": "Hash-based. Sticky.",
    "fix": "Implement A/B testing."
  },
  "313": {
    "desc": "GDPR. PCI-DSS via Stripe Elements.",
    "fix": "Implement compliance measures."
  },
  "314": {
    "desc": "Terraform/Pulumi. Manual FORBIDDEN.",
    "fix": "Use Infrastructure as Code."
  },
  "315": {
    "desc": "ELK/Loki. 30d hot, 1y cold.",
    "fix": "Configure log aggregation."
  },
  "316": {
    "desc": "Grafana. p50/p95/p99.",
    "fix": "Create monitoring dashboards."
  },
  "317": {
    "desc": "P1/P2/P3.",
    "fix": "Configure alerting tiers."
  },
  "318": {
    "desc": "Monthly. 3-month projection.",
    "fix": "Implement capacity planning."
  },
  "319": {
    "desc": "Canary → full. Rollback < 5min.",
    "fix": "Implement safe release process."
  },
  "320": {
    "desc": "< 10min setup. Hot reload.",
    "fix": "Optimize developer experience."
  },
  "321": {
    "desc": "Quarterly reviews.",
    "fix": "Review docs quarterly."
  },
  "322": {
    "desc": "By domain/team via tagging.",
    "fix": "Implement cost allocation."
  },
  "323": {
    "desc": "Least-privilege. 24h offboarding.",
    "fix": "Implement access control."
  },
  "324": {
    "desc": "All via PR + CI.",
    "fix": "Enforce change management."
  },
  "325": {
    "desc": "Right-size. Carbon tracking.",
    "fix": "Implement sustainability practices."
  }
}