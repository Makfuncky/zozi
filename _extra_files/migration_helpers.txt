from __future__ import annotations
"""Thin re-export of the canonical migration helpers in ``utils.migration_helpers``.

Existing Alembic revision scripts do ``from migration_helpers import safe_*``.
The implementation lives in ``utils/migration_helpers.py`` so the helpers are
defined in exactly one place (no duplicate symbol definitions across modules).
"""
from utils.migration_helpers import (  # noqa: F401
    column_exists,
    constraint_exists,
    index_exists,
    safe_add_column,
    safe_create_foreign_key,
    safe_create_index,
    safe_create_table,
    safe_drop_column,
    safe_drop_constraint,
    safe_drop_index,
    safe_drop_table,
    table_exists,
)
