==========================================================================================
FILE: seed_all.py
==========================================================================================
--- around line 292 (showing 282-302) ---
      282:         tables = [
      283:             "internal_emails", "email_folders", "employees",
      284:             "group_chat_messages", "group_chat_members", "group_chat_rooms",
      285:             "direct_chat_messages", "direct_chat_rooms",
      286:             "entity_chat_messages", "entity_chat_threads",
      287:             "order_items", "orders",
      288:             "reviews", "products", "categories",
      289:         ]
      290:         for table in tables:
      291:             try:
>>>   292:                 db.execute(text(f"DELETE FROM {table}"))
      293:             except Exception:
      294:                 pass
      295:         db.commit()
      296:         log.info("  ✅ Existing data wiped")
      297:     except Exception as e:
      298:         log.error(f"  ❌ Wipe failed: {e}")
      299:         db.rollback()
      300:     finally:
      301:         db.close()
      302: 

==========================================================================================
FILE: utils/rls_interceptor.py
==========================================================================================
--- around line 496 (showing 486-503) ---
      486:     from sqlalchemy import text
      487:     from sqlalchemy.sql import quoted_name
      488: 
      489:     safe_schema = quoted_name(_assert_safe_identifier(schema, "schema"), True)
      490:     policy_sql = generate_rls_policy_sql(schema=schema)
      491: 
      492:     with engine.connect().execution_options(isolation_level="AUTOCOMMIT") as conn:
      493:         for table_name in COUNTRY_AWARE_TABLES.keys():
      494:             safe_table = quoted_name(_assert_safe_identifier(table_name, "table"), True)
      495:             conn.execute(
>>>   496:                 text(f"ALTER TABLE {safe_schema}.{safe_table} ENABLE ROW LEVEL SECURITY;")
      497:             )
      498:             conn.execute(text(f"ALTER TABLE {safe_schema}.{safe_table} FORCE ROW LEVEL SECURITY;"))
      499: 
      500:         conn.execute(text(policy_sql))
      501: 
      502:     logger.info("Installed RLS policies for %d tables", len(COUNTRY_AWARE_TABLES))
      503: 

--- around line 498 (showing 488-503) ---
      488: 
      489:     safe_schema = quoted_name(_assert_safe_identifier(schema, "schema"), True)
      490:     policy_sql = generate_rls_policy_sql(schema=schema)
      491: 
      492:     with engine.connect().execution_options(isolation_level="AUTOCOMMIT") as conn:
      493:         for table_name in COUNTRY_AWARE_TABLES.keys():
      494:             safe_table = quoted_name(_assert_safe_identifier(table_name, "table"), True)
      495:             conn.execute(
      496:                 text(f"ALTER TABLE {safe_schema}.{safe_table} ENABLE ROW LEVEL SECURITY;")
      497:             )
>>>   498:             conn.execute(text(f"ALTER TABLE {safe_schema}.{safe_table} FORCE ROW LEVEL SECURITY;"))
      499: 
      500:         conn.execute(text(policy_sql))
      501: 
      502:     logger.info("Installed RLS policies for %d tables", len(COUNTRY_AWARE_TABLES))
      503: 

==========================================================================================
FILE: services/employee_activity_logger.py
==========================================================================================
--- around line 147 (showing 137-157) ---
      137:         params["action_pattern"] = f"%{action_filter}%"
      138:     if since:
      139:         conditions.append("created_at >= :since")
      140:         params["since"] = since
      141:     if until:
      142:         conditions.append("created_at <= :until")
      143:         params["until"] = until
      144: 
      145:     where_clause = " AND ".join(conditions)
      146:     rows = db.execute(
>>>   147:         text(f"""
      148:             SELECT id, actor_employee_id, action, entity_type, entity_id,
      149:                    target_employee_id, country_code, metadata_json, ip_address,
      150:                    device_fingerprint, session_id, created_at
      151:             FROM employee_activity_logs
      152:             WHERE {where_clause}
      153:             ORDER BY created_at DESC
      154:             LIMIT :limit OFFSET :offset
      155:         """),
      156:         params,
      157:     ).mappings().all()

--- around line 270 (showing 260-280) ---
      260:     if country_code:
      261:         conditions.append("country_code = :country")
      262:         params["country"] = country_code
      263:     if since:
      264:         conditions.append("created_at >= :since")
      265:         params["since"] = since
      266: 
      267:     where_clause = " AND ".join(conditions) if conditions else "1=1"
      268: 
      269:     rows = db.execute(
>>>   270:         text(f"""
      271:             SELECT action, COUNT(*) as count
      272:             FROM employee_activity_logs
      273:             WHERE {where_clause}
      274:             GROUP BY action
      275:             ORDER BY count DESC
      276:         """),
      277:         params,
      278:     ).mappings().all()
      279: 
      280:     total = db.execute(

--- around line 281 (showing 271-291) ---
      271:             SELECT action, COUNT(*) as count
      272:             FROM employee_activity_logs
      273:             WHERE {where_clause}
      274:             GROUP BY action
      275:             ORDER BY count DESC
      276:         """),
      277:         params,
      278:     ).mappings().all()
      279: 
      280:     total = db.execute(
>>>   281:         text(f"SELECT COUNT(*) as total FROM employee_activity_logs WHERE {where_clause}"),
      282:         params,
      283:     ).scalar()
      284: 
      285:     return {
      286:         "total_events": total,
      287:         "breakdown": {r["action"]: r["count"] for r in rows},
      288:         "unique_actions": len(rows),
      289:     }
      290: 
      291: 

==========================================================================================
FILE: services/employee_communication_service.py
==========================================================================================
--- around line 502 (showing 492-512) ---
      492:         """),
      493:         {"eid": employee_id, "folder": folder, "limit": limit, "offset": offset},
      494:     ).mappings().all()
      495: 
      496:     sender_ids = [r["sender_id"] for r in rows]
      497:     sender_names = {}
      498:     if sender_ids:
      499:         placeholders = ", ".join(f":sid{i}" for i in range(len(sender_ids)))
      500:         params = {f"sid{i}": sid for i, sid in enumerate(sender_ids)}
      501:         srows = db.execute(
>>>   502:             text(f"""
      503:                 SELECT e.id, u.full_name FROM users u
      504:                 JOIN employees e ON e.user_id = u.id
      505:                 WHERE e.id IN ({placeholders})
      506:             """),
      507:             params,
      508:         ).mappings().all()
      509:         for s in srows:
      510:             sender_names[s["id"]] = s["full_name"]
      511: 
      512:     emails = []

==========================================================================================
FILE: services/financial_reports_service.py
==========================================================================================
--- around line 526 (showing 516-536) ---
      516:         )
      517:         if country_code:
      518:             bal_q = bal_q.filter(AccountBalance.country_code == country_code)
      519:         bal = bal_q.first()
      520:         if bal:
      521:             closing_balance = bal.balance
      522:             cc_where = " AND je.country_code = :cc " if country_code else " "
      523:             cc_p = {"cc": country_code} if country_code else {}
      524:             opening_balance = round_money(
      525:                 Decimal(
>>>   526:                     db.execute(text(f"""
      527:                         SELECT COALESCE(SUM(CASE WHEN jel.side = 'debit' THEN jel.amount ELSE -jel.amount END), 0)
      528:                         FROM journal_entry_lines jel
      529:                         JOIN journal_entries je ON jel.entry_id = je.id
      530:                         WHERE jel.account_id = :aid
      531:                           AND je.entry_date < :ps
      532:                           {cc_where if country_code else ''}
      533:                     """), {"aid": cash_acct.id, "ps": period_start, **cc_p}).scalar() or 0
      534:                 )
      535:             )
      536:             opening_balance = round_money(closing_balance - (

--- around line 537 (showing 527-547) ---
      527:                         SELECT COALESCE(SUM(CASE WHEN jel.side = 'debit' THEN jel.amount ELSE -jel.amount END), 0)
      528:                         FROM journal_entry_lines jel
      529:                         JOIN journal_entries je ON jel.entry_id = je.id
      530:                         WHERE jel.account_id = :aid
      531:                           AND je.entry_date < :ps
      532:                           {cc_where if country_code else ''}
      533:                     """), {"aid": cash_acct.id, "ps": period_start, **cc_p}).scalar() or 0
      534:                 )
      535:             )
      536:             opening_balance = round_money(closing_balance - (
>>>   537:                 db.execute(text(f"""
      538:                     SELECT COALESCE(SUM(CASE WHEN jel.side = 'debit' THEN jel.amount ELSE -jel.amount END), 0)
      539:                     FROM journal_entry_lines jel
      540:                     JOIN journal_entries je ON jel.entry_id = je.id
      541:                     WHERE jel.account_id = :aid
      542:                       AND je.entry_date >= :ps
      543:                       AND je.entry_date <= :pe
      544:                       {cc_where if country_code else ''}
      545:                 """), {"aid": cash_acct.id, "ps": period_start, "pe": period_end, **cc_p}).scalar() or 0
      546:             ))
      547: 

--- around line 582 (showing 572-592) ---
      572:         for a in acct_rows:
      573:             acct_by_code[a.code] = a
      574: 
      575:     acct_ids = [a.id for a in acct_by_code.values() if a]
      576:     change_by_acct = {}
      577:     if acct_ids:
      578:         placeholders = ", ".join(f":aid{i}" for i in range(len(acct_ids)))
      579:         params = {f"aid{i}": aid for i, aid in enumerate(acct_ids)}
      580:         params.update({"ps": period_start, "pe": period_end, **cc_p})
      581:         change_rows = db.execute(
>>>   582:             text(f"""
      583:                 SELECT jel.account_id,
      584:                        COALESCE(SUM(CASE WHEN jel.side = 'debit' THEN jel.amount ELSE -jel.amount END), 0) as change
      585:                 FROM journal_entry_lines jel
      586:                 JOIN journal_entries je ON jel.entry_id = je.id
      587:                 WHERE jel.account_id IN ({placeholders})
      588:                   AND je.entry_date >= :ps
      589:                   AND je.entry_date <= :pe
      590:             """ + cc_where),
      591:             params,
      592:         ).mappings().all()

==========================================================================================
FILE: services/performance_service.py
==========================================================================================
--- around line 333 (showing 323-343) ---
      323:         """),
      324:         {"eid": employee_id},
      325:     ).mappings().all()
      326: 
      327:     objective_ids = [o["id"] for o in objectives]
      328:     kpis_by_obj = {}
      329:     if objective_ids:
      330:         placeholders = ", ".join(f":oid{i}" for i in range(len(objective_ids)))
      331:         params = {f"oid{i}": oid for i, oid in enumerate(objective_ids)}
      332:         kpi_rows = db.execute(
>>>   333:             text(f"""
      334:                 SELECT objective_id, id, metric_name, target_value, current_value, metric_type as unit, weight_pct as weight, last_recorded_at
      335:                 FROM kpi_metrics
      336:                 WHERE objective_id IN ({placeholders})
      337:                 ORDER BY weight_pct DESC
      338:             """),
      339:             params,
      340:         ).mappings().all()
      341:         for k in kpi_rows:
      342:             kpis_by_obj.setdefault(k["objective_id"], []).append(dict(k))
      343: 

==========================================================================================
FILE: routers/admin_logistics_operations.py
==========================================================================================
--- around line 2065 (showing 2055-2075) ---
     2055:         "promotion_order_tiers",
     2056:         "product_variants",
     2057:         "products",
     2058:         "categories",
     2059:         "audit_logs",
     2060:         "notifications",
     2061:     ]
     2062:     deleted_counts: dict[str, int] = {}
     2063:     for table in tables_to_clear:
     2064:         try:
>>>  2065:             result = db.execute(text(f"DELETE FROM {table}"))
     2066:             deleted_counts[table] = result.rowcount or 0
     2067:         except Exception:
     2068:             # Table may not exist in this schema version
     2069:             deleted_counts[table] = -1
     2070: 
     2071:     # Delete all non-admin users (preserve admin accounts)
     2072:     non_admin_count = db.execute(
     2073:         text("DELETE FROM users WHERE role != 'admin'")
     2074:     ).rowcount or 0
     2075:     deleted_counts["users_(non_admin)"] = non_admin_count

==========================================================================================
FILE: routers/employee_hr_health.py
==========================================================================================
--- around line 85 (showing 75-95) ---
       75:     if country_code:
       76:         enforce_country_access(country_code, db=db)
       77:         country_filter = " AND country_code = :country_code"
       78:         params["country_code"] = country_code
       79: 
       80:     now = _utcnow()
       81: 
       82:     # ── Onboarding Pipeline Stats ──
       83:     try:
       84:         pipeline_counts = db.execute(
>>>    85:             text(f"""
       86:                 SELECT
       87:                     SUM(CASE WHEN status = 'in_progress' THEN 1 ELSE 0 END) as active,
       88:                     SUM(CASE WHEN status = 'in_progress' AND due_date < :now THEN 1 ELSE 0 END) as overdue,
       89:                     SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) as completed,
       90:                     SUM(CASE WHEN status = 'cancelled' THEN 1 ELSE 0 END) as cancelled
       91:                 FROM onboarding_pipelines
       92:                 WHERE 1=1 {country_filter}
       93:             """),
       94:             {**params, "now": now},
       95:         ).mappings().first()

--- around line 98 (showing 88-108) ---
       88:                     SUM(CASE WHEN status = 'in_progress' AND due_date < :now THEN 1 ELSE 0 END) as overdue,
       89:                     SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) as completed,
       90:                     SUM(CASE WHEN status = 'cancelled' THEN 1 ELSE 0 END) as cancelled
       91:                 FROM onboarding_pipelines
       92:                 WHERE 1=1 {country_filter}
       93:             """),
       94:             {**params, "now": now},
       95:         ).mappings().first()
       96: 
       97:         overdue_items = db.execute(
>>>    98:             text(f"""
       99:                 SELECT p.id, p.employee_id, p.current_step, p.total_steps,
      100:                        p.completed_steps, p.due_date,
      101:                        e.employee_code, e.department, e.position
      102:                 FROM onboarding_pipelines p
      103:                 LEFT JOIN employees e ON e.id = p.employee_id
      104:                 WHERE p.status = 'in_progress' AND p.due_date < :now {country_filter}
      105:                 ORDER BY p.due_date ASC
      106:                 LIMIT 20
      107:             """),
      108:             {**params, "now": now},

--- around line 122 (showing 112-132) ---
      112:             "stats": dict(pipeline_counts) if pipeline_counts else {"active": 0, "overdue": 0, "completed": 0, "cancelled": 0},
      113:             "overdue_items": [dict(r) for r in overdue_items],
      114:         }
      115:     except Exception as e:
      116:         logger.warning("Onboarding data unavailable (migration may not be run): %s", e)
      117:         result["onboarding"] = {"stats": {"active": 0, "overdue": 0, "completed": 0, "cancelled": 0}, "overdue_items": []}
      118: 
      119:     # ── Performance Health Board ──
      120:     try:
      121:         health_data = db.execute(
>>>   122:             text(f"""
      123:                 SELECT
      124:                     SUM(CASE WHEN performance_score >= 4.0 THEN 1 ELSE 0 END) as green,
      125:                     SUM(CASE WHEN performance_score >= 2.5 AND performance_score < 4.0 THEN 1 ELSE 0 END) as amber,
      126:                     SUM(CASE WHEN performance_score < 2.5 AND performance_score IS NOT NULL THEN 1 ELSE 0 END) as red,
      127:                     SUM(CASE WHEN performance_score IS NULL THEN 1 ELSE 0 END) as not_scored,
      128:                     ROUND(AVG(performance_score), 2) as avg_score
      129:                 FROM employees
      130:                 WHERE employment_status = 'active' {country_filter}
      131:             """),
      132:             params,

--- around line 136 (showing 126-146) ---
      126:                     SUM(CASE WHEN performance_score < 2.5 AND performance_score IS NOT NULL THEN 1 ELSE 0 END) as red,
      127:                     SUM(CASE WHEN performance_score IS NULL THEN 1 ELSE 0 END) as not_scored,
      128:                     ROUND(AVG(performance_score), 2) as avg_score
      129:                 FROM employees
      130:                 WHERE employment_status = 'active' {country_filter}
      131:             """),
      132:             params,
      133:         ).mappings().first()
      134: 
      135:         top_performers = db.execute(
>>>   136:             text(f"""
      137:                 SELECT e.id, e.employee_code, e.department, e.position, e.performance_score
      138:                 FROM employees e
      139:                 WHERE e.employment_status = 'active'
      140:                   AND e.performance_score IS NOT NULL {country_filter}
      141:                 ORDER BY e.performance_score DESC
      142:                 LIMIT 10
      143:             """),
      144:             params,
      145:         ).mappings().all()
      146: 

--- around line 148 (showing 138-158) ---
      138:                 FROM employees e
      139:                 WHERE e.employment_status = 'active'
      140:                   AND e.performance_score IS NOT NULL {country_filter}
      141:                 ORDER BY e.performance_score DESC
      142:                 LIMIT 10
      143:             """),
      144:             params,
      145:         ).mappings().all()
      146: 
      147:         bottom_performers = db.execute(
>>>   148:             text(f"""
      149:                 SELECT e.id, e.employee_code, e.department, e.position, e.performance_score
      150:                 FROM employees e
      151:                 WHERE e.employment_status = 'active'
      152:                   AND e.performance_score IS NOT NULL {country_filter}
      153:                 ORDER BY e.performance_score ASC
      154:                 LIMIT 5
      155:             """),
      156:             params,
      157:         ).mappings().all()
      158: 

--- around line 174 (showing 164-184) ---
      164:     except Exception as e:
      165:         logger.warning("Performance data unavailable (performance_score column may not exist): %s", e)
      166:         result["performance"] = {"stats": {"green": 0, "amber": 0, "red": 0, "not_scored": 0, "avg_score": None}, "top_performers": [], "bottom_performers": []}
      167: 
      168:     # ── Recent Activity Feed ──
      169:     try:
      170:         since_date = now.replace(hour=0, minute=0, second=0, microsecond=0)
      171:         params["since"] = since_date
      172: 
      173:         activity = db.execute(
>>>   174:             text(f"""
      175:                 SELECT al.id, al.actor_employee_id, al.action, al.entity_type,
      176:                        al.entity_id, al.target_employee_id, al.metadata_json,
      177:                        al.created_at,
      178:                        ae.employee_code as actor_code,
      179:                        te.employee_code as target_code
      180:                 FROM employee_activity_logs al
      181:                 LEFT JOIN employees ae ON ae.id = al.actor_employee_id
      182:                 LEFT JOIN employees te ON te.id = al.target_employee_id
      183:                 WHERE al.created_at >= :since {country_filter}
      184:                 ORDER BY al.created_at DESC

==========================================================================================
FILE: services/command_center_background.py
==========================================================================================
--- around line 57 (showing 47-67) ---
       47:                 self.calculate_and_cache_demographics(),
       48:                 self.calculate_and_cache_country_sales(),
       49:                 self.calculate_and_cache_search_trends(),
       50:                 self.calculate_and_cache_employee_metrics(),
       51:                 self.calculate_and_cache_fraud_metrics(),
       52:                 self.calculate_and_cache_system_health(),
       53:                 return_exceptions=True
       54:             )
       55:             logger.info("Completed Command Center cache update jobs")
       56:         except Exception as e:
>>>    57:             logger.error(f"Error in cache update jobs: {e}")
       58:     
       59:     async def calculate_and_cache_demographics(self) -> None:
       60:         """Calculate and cache demographic data."""
       61:         with get_service_session() as db:
       62:             try:
       63:                 gender_result = db.execute(text(
       64:                     """
       65:                     SELECT 
       66:                         'unknown' as gender, 
       67:                         COUNT(*) as count

==========================================================================================
FILE: services/fraud_detection_service.py
==========================================================================================
--- around line 944 (showing 934-954) ---
      934:                         self.redis.execute_command("BF.ADD", key, ip)
      935:                         count += 1
      936:                     except redis.exceptions.ResponseError:
      937:                         pass
      938:             
      939:             self.db.execute(text("UPDATE ip_reputation SET is_tor = true WHERE ip_address IN (:ips)"), 
      940:                           {"ips": []})
      941:             self.db.commit()
      942:             return count
      943:         except Exception as e:
>>>   944:             logger.error(f"Failed to update Tor list: {e}")
      945:             return 0
      946:     
      947:     def _update_proxy_list(self) -> int:
      948:         """Update proxy IP list."""
      949:         count = 0
      950:         try:
      951:             key = "fraud:bloom:proxies"
      952:             proxies = [
      953:                 "1.1.1.1", "8.8.8.8", "9.9.9.9",
      954:             ]

==========================================================================================
FILE: services/logistics_engine.py
==========================================================================================
--- around line 160 (showing 150-170) ---
      150:         if existing:
      151:             existing.update(normalized)
      152:         else:
      153:             providers.append(normalized)
      154: 
      155:         country.logistics_providers_json = json.dumps(providers, default=str)
      156:         self.db.commit()
      157:         self.db.refresh(country)
      158: 
      159:         return {
>>>   160:             "message": f"Provider '{provider_id}' {'updated' if existing else 'created'} for {country_code}",
      161:             "provider": normalized,
      162:         }
      163: 
      164:     # ── Internal helpers ───────────────────────────────────────────────────
      165: 
      166:     def _get_country(self, country_code: str) -> CountryConfig | None:
      167:         return self.db.query(CountryConfig).filter(
      168:             CountryConfig.code == normalize_country_code(country_code),
      169:             CountryConfig.is_active == True,
      170:         ).first()

==========================================================================================
FILE: services/order_tracking_service.py
==========================================================================================
--- around line 737 (showing 727-747) ---
      727:             "postal_code": order.shipping_postal_code,
      728:             "delivery_location": order.delivery_location,
      729:             "delivery_note": order.delivery_note,
      730:         },
      731:         "items": [
      732:             {
      733:                 "product_name": item.product_name or f"Product #{item.product_id}",
      734:                 "quantity": item.quantity,
      735:                 "price": float(item.price or 0),
      736:                 "total": float((item.price or 0) * item.quantity),
>>>   737:                 "variant": f"{item.selected_size or ''} {item.selected_color or ''}".strip() or None,
      738:             }
      739:             for item in items
      740:         ],
      741:         "totals": {
      742:             "subtotal": float(order.subtotal_amount or 0),
      743:             "shipping": float(order.shipping_amount or 0),
      744:             "discount": float(order.discount_amount or 0),
      745:             "tax": float(order.tax_amount or 0),
      746:             "total": float(order.total_amount or 0),
      747:         },

--- around line 375 (showing 365-385) ---
      365:     logistics_received, distribution_checkpoint, out_for_delivery,
      366:     shipment_delayed, shipment_failed, shipment_rescheduled."""
      367:     if event_type not in LOGISTICS_SUB_STATUSES + FAULT_STATUSES:
      368:         return {"success": False, "error": f"Invalid event type: {event_type}"}
      369: 
      370:     order, shipment = _get_order_for_logistics(db, order_id, logistics_user_id)
      371:     if not order or not shipment:
      372:         return {"success": False, "error": "Order or shipment not found"}
      373: 
      374:     if order.status not in ("shipped", "in_transit"):
>>>   375:         return {"success": False, "error": f"Cannot update transit in '{order.status}' status"}
      376: 
      377:     # Map event type to status
      378:     status_map = {
      379:         "logistics_received": "in_transit",
      380:         "distribution_checkpoint": "in_transit",
      381:         "out_for_delivery": "in_transit",
      382:         "shipment_delayed": "in_transit",
      383:         "shipment_failed": "failed",
      384:         "shipment_rescheduled": "in_transit",
      385:     }

==========================================================================================
FILE: services/permission_service.py
==========================================================================================
--- around line 77 (showing 67-87) ---
       67: 
       68: def update_category(category_id: int, data: dict, actor_id: int, db: Session) -> Optional[PermissionCategory]:
       69:     category = db.query(PermissionCategory).filter(PermissionCategory.id == category_id).first()
       70:     if not category:
       71:         return None
       72:     for key in ("name", "slug", "description", "icon", "sort_order", "is_active"):
       73:         if key in data:
       74:             setattr(category, key, data[key])
       75:     db.commit()
       76:     db.refresh(category)
>>>    77:     _log_audit(actor_id, "category_updated", target_role=None, permission_id=None, country_code=None, details=f"Updated category '{category.name}'", db=db)
       78:     return category
       79: 
       80: 
       81: def delete_category(category_id: int, actor_id: int, db: Session) -> bool:
       82:     category = db.query(PermissionCategory).filter(PermissionCategory.id == category_id).first()
       83:     if not category:
       84:         return False
       85:     _log_audit(actor_id, "category_deleted", target_role=None, permission_id=None, country_code=None, details=f"Deleted category '{category.name}'", db=db)
       86:     db.delete(category)
       87:     db.commit()

--- around line 85 (showing 75-95) ---
       75:     db.commit()
       76:     db.refresh(category)
       77:     _log_audit(actor_id, "category_updated", target_role=None, permission_id=None, country_code=None, details=f"Updated category '{category.name}'", db=db)
       78:     return category
       79: 
       80: 
       81: def delete_category(category_id: int, actor_id: int, db: Session) -> bool:
       82:     category = db.query(PermissionCategory).filter(PermissionCategory.id == category_id).first()
       83:     if not category:
       84:         return False
>>>    85:     _log_audit(actor_id, "category_deleted", target_role=None, permission_id=None, country_code=None, details=f"Deleted category '{category.name}'", db=db)
       86:     db.delete(category)
       87:     db.commit()
       88:     return True
       89: 
       90: 
       91: # ── Permission CRUD ───────────────────────────────────────────────
       92: 
       93: 
       94: def list_permissions(db: Session, category_id: Optional[int] = None) -> list[dict]:
       95:     q = db.query(Permission)

==========================================================================================
FILE: services/transactional_email_service.py
==========================================================================================
--- around line 146 (showing 136-156) ---
      136:         order = getattr(req, "order", None)
      137:         user = getattr(order, "user", None)
      138:         if req is None or order is None or user is None or not getattr(user, "email", None):
      139:             return {"return_id": return_id, "sent": False, "reason": "missing-return-or-user"}
      140: 
      141:         if event_kind == "created":
      142:             subject = f"Return request received for order #{order.id}"
      143:             detail = f"We received your {getattr(req, 'intent', 'return')} request and the team will review it shortly."
      144:         else:
      145:             status = str(getattr(req, "status", "pending") or "pending").replace("_", " ").title()
>>>   146:             subject = f"Return request update for order #{order.id}"
      147:             detail = f"Your return request is now <strong>{status}</strong>."
      148: 
      149:         html = (
      150:             f"<h2>Hi {_first_name(user)},</h2>"
      151:             f"<p>{detail}</p>"
      152:             f"<p><strong>Reason:</strong> {getattr(req, 'reason', '')}</p>"
      153:             f"<p><a href=\"{_order_link(cast(int, order.id))}\">View order</a></p>"
      154:         )
      155:         send_email(str(user.email), subject, html, purpose="notification")
      156:         return {"return_id": return_id, "sent": True, "event_kind": event_kind}

==========================================================================================
FILE: scripts/add_schema_to_models.py
==========================================================================================
--- around line 100 (showing 90-107) ---
       90:             # No __table_args__ exists - add one after __tablename__
       91:             # Insert after the __tablename__ line
       92:             tablename_end = class_end
       93:             insert_point = content.find('\n', tablename_end) + 1
       94:             new_table_args = f'    __table_args__ = {{"schema": "{schema}"}}\n'
       95:             content = content[:insert_point] + new_table_args + content[insert_point:]
       96:             replacements += 1
       97: 
       98:     if content != original:
       99:         file_path.write_text(content)
>>>   100:         print(f"Updated {file_path.name}: {replacements} classes")
      101:     else:
      102:         print(f"No changes: {file_path.name}")
      103: 
      104: for model_file in sorted(models_dir.glob('*.py')):
      105:     if model_file.name in ('__init__.py', 'mixins.py'):
      106:         continue
      107:     update_model_file(model_file)

==========================================================================================
FILE: routers/admin_identity_operations.py
==========================================================================================
--- around line 120 (showing 110-130) ---
      110:         role = payload.get("role")
      111:         if not user_ids or not role:
      112:             raise HTTPException(400, "user_ids and role are required")
      113:         updated = 0
      114:         for uid in user_ids:
      115:             try:
      116:                 update_user_role(uid, role, {"id": current_user.id, "username": current_user.username, "role": current_user.role}, db)
      117:                 updated += 1
      118:             except HTTPException:
      119:                 pass
>>>   120:         return {"message": f"Updated {updated} users", "updated": updated}
      121:     finally:
      122:         clear_rls_context()
      123: 
      124: 
      125: @router.post("/users/{country_code}/bulk-toggle-active")
      126: def bulk_toggle_user_active(country_code: str = Path(..., description="ISO country code"), payload: dict = Body(...), _: User = Depends(require_admin), db: Session = Depends(get_db), current_user: User = Depends(require_admin)):
      127:     get_country_or_404(country_code.upper(), db)
      128:     set_rls_context({country_code.upper()}, is_restricted=True)
      129:     try:
      130:         user_ids = payload.get("user_ids", [])

==========================================================================================
FILE: routers/admin_orders_status.py
==========================================================================================
--- around line 182 (showing 172-192) ---
      172:     get_country_or_404(country_code.upper(), db)
      173:     set_rls_context({country_code.upper()}, is_restricted=True)
      174:     try:
      175:         updated = 0
      176:         for oid in payload.ids:
      177:             o = db.query(Order).filter(Order.id == oid).first()
      178:             if o:
      179:                 o.status = payload.status
      180:                 updated += 1
      181:         db.commit()
>>>   182:         return {"message": f"Status updated for {updated} orders", "updated": updated}
      183:     finally:
      184:         from utils.rls_interceptor import clear_rls_context
      185:         clear_rls_context()
      186: 
      187: 
      188: @router.delete("/orders/{country_code}/{order_id}")
      189: def delete_order_permanent(
      190:     country_code: str = Path(..., description="ISO country code"),
      191:     order_id: int = Path(...),
      192:     _: User = Depends(require_super_admin),

==========================================================================================
FILE: routers/logistics_reporting.py
==========================================================================================
--- around line 263 (showing 253-273) ---
      253:     return result
      254: 
      255: 
      256: @router.post("/backfill-authority-levels")
      257: def refresh_authority_levels(
      258:     db: Session = Depends(get_db),
      259:     current_user: dict = Depends(get_current_user),
      260: ):
      261:     updated = backfill_authority_levels(db)
      262:     db.commit()
>>>   263:     return {"message": f"Updated {updated} employee authority levels"}
      264: 
      265: 
      266: # ══════════════════════════════════════════════════════════════════
      267: #  Matrix / Dotted-Line Management
      268: # ══════════════════════════════════════════════════════════════════
      269: 
      270: 
      271: @router.post("/matrix/assign")
      272: def assign_matrix(
      273:     payload: MatrixAssign,

==========================================================================================
FILE: providers/legacy/br_08.py
==========================================================================================
--- around line 376 (showing 366-386) ---
      366:                     
      367:                     if score > 0.85:
      368:                         logger.info(f"  ✓ Excellent score ({score:.3f}), stopping model tests")
      369:                         break
      370:             else:
      371:                 logger.warning(f"  ✗ {model_name} failed, trying next model...")
      372:             
      373:             gc.collect()
      374:         
      375:         if best_prob_map is not None:
>>>   376:             logger.info(f"✓ Selected best model: {best_model} (score: {best_score:.3f}, tested {models_tried} models)")
      377:             return best_prob_map, best_model
      378:         
      379:         return None, ""
      380: 
      381: # ========================== STAGE 6-15: POST-PROCESSING ==========================
      382: class HoleFiller:
      383:     @staticmethod
      384:     def fill(image_rgba: np.ndarray, subject_type: str = None) -> np.ndarray:
      385:         alpha = image_rgba[:, :, 3]
      386:         visible_mask = (alpha > 128).astype(np.uint8) * 255

==========================================================================================
FILE: middleware/behavioral_analytics.py
==========================================================================================
--- around line 103 (showing 93-113) ---
       93: 
       94:     def _add_to_history(self, key: str, value: float):
       95:         """Add value to history in Redis."""
       96:         if not self.redis:
       97:             return
       98: 
       99:         try:
      100:             self.redis.lpush(key, value)
      101:             self.redis.ltrim(key, 0, self.window_size - 1)
      102:         except Exception as e:
>>>   103:             logger.warning(f"Risk score update failed: {e}")
      104:             pass
      105: 
      106:     def _calculate_z_score(self, value: float, history: List[float]) -> float:
      107:         """Calculate Z-score for value against history."""
      108:         if len(history) < 2:
      109:             return 0.0
      110: 
      111:         mean = statistics.mean(history)
      112:         stdev = statistics.stdev(history)
      113: 

==========================================================================================
FILE: middleware/rate_limit_middleware.py
==========================================================================================
--- around line 225 (showing 215-234) ---
      215:                 current = float(current)
      216:                 last_update = now - ttl
      217: 
      218:             tokens_to_add = (now - last_update) * self.rate
      219:             current = min(self.capacity, current + tokens_to_add)
      220: 
      221:             if current >= tokens:
      222:                 current -= tokens
      223:                 redis.mset({
      224:                     bucket_key: current,
>>>   225:                     f"last_update:{bucket_key}": now
      226:                 })
      227:                 redis.expire(bucket_key, int(self.config.window_size))
      228:                 return True, int(current), 0
      229:             else:
      230:                 retry_after = math.ceil((tokens - current) / self.rate)
      231:                 return False, int(current), retry_after
      232: 
      233:         except Exception:
      234:             return True, self.capacity - tokens, 0

==========================================================================================
FILE: jobs/threat_feed_updater.py
==========================================================================================
--- around line 52 (showing 42-58) ---
       42:     return set()
       43: 
       44: async def update_threat_feeds():
       45:     """Update all threat feeds and return counts."""
       46:     results = {}
       47:     
       48:     async with aiohttp.ClientSession() as session:
       49:         for feed_name, feed_config in THREAT_FEEDS.items():
       50:             ips = await fetch_ip_list(session, feed_config["url"])
       51:             results[feed_name] = len(ips)
>>>    52:             logger.info(f"Updated {feed_name}: {len(ips)} entries")
       53:     
       54:     return results
       55: 
       56: if __name__ == "__main__":
       57:     results = asyncio.run(update_threat_feeds())
       58:     print(f"Threat feed update complete: {results}")

==========================================================================================
FILE: controllers/cart_controller.py
==========================================================================================
--- around line 91 (showing 81-101) ---
       81:         .options(joinedload(CartItem.product).selectinload(Product.variants))
       82:         .filter(CartItem.user_id == user_id)
       83:         .all()
       84:     )
       85: 
       86: 
       87: def _resolve_variant_or_raise(product: Product, selected_size: str | None, selected_color: str | None):
       88:     variant = resolve_product_variant(product, selected_size, selected_color)
       89:     has_variants = bool(getattr(product, "variants", []) or [])
       90:     if has_variants and (_normalize_variant(selected_size) or _normalize_variant(selected_color)) and variant is None:
>>>    91:         raise HTTPException(status_code=422, detail=f"Selected variant is not available for '{product.name}'")
       92:     return variant
       93: 
       94: 
       95: # ── Public API ────────────────────────────────────────────────────────────────
       96: 
       97: def get_cart(user_id: int, db: Session) -> List[dict]:
       98:     items = _load_cart_items(user_id, db)
       99:     # Filter out deleted/inactive products silently
      100:     return [_cart_row(i) for i in items if i.product and not i.product.is_deleted]
      101: 

==========================================================================================
FILE: controllers/logistics_partner_controller.py
==========================================================================================
--- around line 3182 (showing 3172-3192) ---
     3172:             ("processing", "picking_up"): "pickup_confirmed",
     3173:             ("picking_up", "processing"): "pickup_cancelled",
     3174:             ("picking_up", "shipped"): "picked_from_supplier",
     3175:             ("shipped", "in_transit"): "distribution_checkpoint",
     3176:             ("shipped", "delivered"): "customer_received",
     3177:             ("in_transit", "delivered"): "customer_received",
     3178:             ("shipped", "failed"): "shipment_failed",
     3179:             ("in_transit", "failed"): "shipment_failed",
     3180:             ("shipped", "returned"): "shipment_returned",
     3181:             ("in_transit", "returned"): "shipment_returned",
>>>  3182:         }.get((old_status, new_status), f"status_updated_to_{new_status}")
     3183: 
     3184:     event_notes = " · ".join(
     3185:         part for part in [str(data.get("notes", "")).strip() or None, vehicle_selection_note] if part
     3186:     ) or None
     3187:     event = ShipmentEvent(
     3188:         shipment_id=shipment.id,
     3189:         order_id=shipment.order_id,
     3190:         supplier_id=shipment.supplier_id,
     3191:         actor_user_id=current_user["id"],
     3192:         actor_role=role,

==========================================================================================
FILE: controllers/orders_controller.py
==========================================================================================
--- around line 181 (showing 171-191) ---
      171:         ).with_for_update().all()
      172:     }
      173: 
      174:     for item in order.items:
      175:         product = products.get(item.product_id)
      176:         if not product:
      177:             raise HTTPException(status_code=404, detail=f"Product {item.product_id} not found")
      178:         variant = resolve_product_variant(product, item.selected_size, item.selected_color)
      179:         has_variants = bool(getattr(product, "variants", []) or [])
      180:         if has_variants and ((item.selected_size or "").strip() or (item.selected_color or "").strip()) and variant is None:
>>>   181:             raise HTTPException(status_code=422, detail=f"Selected variant is not available for '{product.name}'")
      182: 
      183:     for product_id, requested_quantity in requested_quantities.items():
      184:         product = products.get(product_id)
      185:         if not product:
      186:             raise HTTPException(status_code=404, detail=f"Product {product_id} not found")
      187:         matching_variants = [
      188:             resolve_product_variant(product, item.selected_size, item.selected_color)
      189:             for item in order.items
      190:             if item.product_id == product_id
      191:         ]

==========================================================================================
FILE: controllers/supplier_controller.py
==========================================================================================
--- around line 1070 (showing 1060-1080) ---
     1060:         "confirmed": ["processing"],
     1061:         "processing": ["prepared"],
     1062:         "prepared": ["processing"],
     1063:     }
     1064: 
     1065:     if new_status in ALLOWED_SUPPLIER_TRANSITIONS.get(current_reconciled, []):
     1066:         order.status = new_status
     1067:         db.commit()
     1068:         db.refresh(order)
     1069:         return {
>>>  1070:             "message": f"Order status updated to '{new_status}'",
     1071:             "order_id": order.id,
     1072:             "status": order.status,
     1073:         }
     1074: 
     1075:     raise HTTPException(
     1076:         status_code=409,
     1077:         detail=(
     1078:             f"Cannot transition order status from '{current_reconciled}' to '{new_status}'. "
     1079:             "Supplier can only transition: confirmed -> processing -> shipped. "
     1080:             "Other status transitions (picking_up, in_transit, delivered) are derived "

==========================================================================================
FILE: services/chat_enrichment.py
==========================================================================================
--- around line 154 (showing 144-164) ---
      144:         "direct": "direct_chat_messages",
      145:         "group": "group_chat_messages",
      146:         "channel": "internal_messages",
      147:     }
      148:     table = tables.get(message_type)
      149:     if not table:
      150:         raise ValueError(f"Unknown message type: {message_type}")
      151: 
      152:     # Get original
      153:     original = db.execute(
>>>   154:         text(f"SELECT id, sender_id, body FROM {table} WHERE id = :id"),
      155:         {"id": message_id},
      156:     ).mappings().first()
      157:     if not original:
      158:         raise ValueError("Message not found")
      159:     if original["sender_id"] != employee_id:
      160:         raise ValueError("Can only edit your own messages")
      161: 
      162:     # Audit log the original
      163:     db.execute(
      164:         text("""

--- around line 183 (showing 173-193) ---
      173:             "entity_id": message_id,
      174:             "user_id": employee_id,
      175:             "preview": original["body"][:200],
      176:             "metadata": json.dumps({"original_body": original["body"], "new_body": new_body}),
      177:             "now": _utcnow(),
      178:         },
      179:     )
      180: 
      181:     # Update
      182:     db.execute(
>>>   183:         text(f"UPDATE {table} SET body = :body WHERE id = :id"),
      184:         {"body": new_body, "id": message_id},
      185:     )
      186:     db.commit()
      187:     _log_activity(db, employee_id, "chat_message_edited", f"chat_{message_type}", str(message_id))
      188:     return {"id": message_id, "body": new_body, "edited": True}
      189: 
      190: 
      191: def delete_message(
      192:     db: Session,
      193:     message_id: int,

--- around line 235 (showing 225-245) ---
      225:             "entity_type": f"{message_type}_message",
      226:             "entity_id": message_id,
      227:             "user_id": employee_id,
      228:             "preview": original["body"][:200],
      229:             "metadata": json.dumps({"deleted_by": employee_id, "hard_delete": hard_delete}),
      230:             "now": _utcnow(),
      231:         },
      232:     )
      233: 
      234:     if hard_delete:
>>>   235:         db.execute(text(f"DELETE FROM {table} WHERE id = :id"), {"id": message_id})
      236:     else:
      237:         db.execute(
      238:             text(f"UPDATE {table} SET body = '[deleted]', is_deleted = TRUE WHERE id = :id"),
      239:             {"id": message_id},
      240:         )
      241:     db.commit()
      242:     _log_activity(db, employee_id, "chat_message_deleted", f"chat_{message_type}", str(message_id))
      243:     return {"id": message_id, "deleted": True, "hard_delete": hard_delete}
      244: 
      245: 

==========================================================================================
FILE: services/hr/ess_write_service.py
==========================================================================================
--- around line 47 (showing 37-57) ---
       37:     if emergency_contact_name is not None:
       38:         updates.append("emergency_contact_name = :ec_name")
       39:         params["ec_name"] = emergency_contact_name
       40:     if emergency_contact_phone is not None:
       41:         updates.append("emergency_contact_phone = :ec_phone")
       42:         params["ec_phone"] = emergency_contact_phone
       43:     if not updates:
       44:         raise HTTPException(status_code=400, detail="No fields to update")
       45:     # Column names come from a fixed allowlist, never from user input.
       46:     db.execute(
>>>    47:         text(f"UPDATE employees SET {', '.join(updates)} WHERE id = :eid"),
       48:         params,
       49:     )
       50:     db.commit()
       51:     log_activity(db, emp.id, "profile_updated", "employee_profile", str(emp.id))
       52:     return {"status": "updated", "fields": [u.split(" =")[0] for u in updates]}
       53: 
       54: 
       55: def create_leave_request(
       56:     db: Session,
       57:     emp: Employee,

==========================================================================================
FILE: scripts/add_schema_declarations.py
==========================================================================================
--- around line 139 (showing 129-149) ---
      129:                             if isinstance(item.value, ast.Constant) and isinstance(item.value.value, str):
      130:                                 tablename = item.value.value
      131: 
      132:             if not tablename:
      133:                 continue
      134: 
      135:             schema = SCHEMA_MAPPING.get(tablename)
      136:             if not schema or schema == "public":
      137:                 continue
      138: 
>>>   139:             if f'"schema": "{schema}"' in updated or f"'schema': '{schema}'" in updated:
      140:                 continue
      141: 
      142:             updated = ensure_schema_in_table_args(updated, tablename, schema)
      143: 
      144:         if updated != original:
      145:             py.write_text(updated, encoding="utf-8")
      146:             modified_files.add(py.name)
      147: 
      148:     print(f"Modified {len(modified_files)} model files:")
      149:     for name in sorted(modified_files):

==========================================================================================
FILE: scripts/add_schema_declarations_debug.py
==========================================================================================
--- around line 134 (showing 124-144) ---
      124:                             if isinstance(item.value, ast.Constant) and isinstance(item.value.value, str):
      125:                                 tablename = item.value.value
      126: 
      127:             if not tablename:
      128:                 continue
      129: 
      130:             schema = SCHEMA_MAPPING.get(tablename)
      131:             if not schema or schema == "public":
      132:                 continue
      133: 
>>>   134:             if f'"schema": "{schema}"' in updated or f"'schema': '{schema}'" in updated:
      135:                 continue
      136: 
      137:             updated = ensure_schema_in_table_args(updated, tablename, schema)
      138: 
      139:         if updated != original:
      140:             py.write_text(updated, encoding="utf-8")
      141:             modified_files.append(py.name)
      142:             print(f"Modified: {py.name}")
      143:         else:
      144:             print(f"Unchanged: {py.name}")

==========================================================================================
FILE: routers/customer_payments.py
==========================================================================================
--- around line 81 (showing 71-91) ---
       71:         params["address"] = address
       72:     if emergency_contact_name is not None:
       73:         updates.append("emergency_contact_name = :ec_name")
       74:         params["ec_name"] = emergency_contact_name
       75:     if emergency_contact_phone is not None:
       76:         updates.append("emergency_contact_phone = :ec_phone")
       77:         params["ec_phone"] = emergency_contact_phone
       78:     if not updates:
       79:         raise HTTPException(status_code=400, detail="No fields to update")
       80:     db.execute(
>>>    81:         text(f"UPDATE employees SET {', '.join(updates)} WHERE id = :eid"),
       82:         params,
       83:     )
       84:     db.commit()
       85:     log_activity(db, emp.id, "profile_updated", "employee_profile", str(emp.id))
       86:     return {"status": "updated", "fields": [u.split(" =")[0] for u in updates]}
       87: 
       88: 
       89: # ── Leave ────────────────────────────────────────────────────────
       90: 
       91: 

==========================================================================================
FILE: controllers/command_center_controller.py
==========================================================================================
--- around line 57 (showing 47-67) ---
       47: def safe_fetch(db: Session, sql: str, params: dict | None = None, scalar: bool = False) -> Any:
       48:     try:
       49:         result = db.execute(text(sql), params or {})
       50:         return result.scalar() if scalar else result.fetchall()
       51:     except Exception:
       52:         return 0 if scalar else []
       53: 
       54: 
       55: def safe_count(db: Session, table: str, where: str = "1=1", params: dict | None = None) -> Any:
       56:     validated_table = _validate_table_name(table)
>>>    57:     return safe_fetch(db, f"SELECT COUNT(*) FROM {validated_table} WHERE {where}", params, scalar=True)
       58: 
       59: 
       60: router = APIRouter()
       61: 
       62: 
       63: class SystemMetricsResponse(BaseModel):
       64:     api_latency_p95: float
       65:     api_latency_p99: float
       66:     error_rate: float
       67:     db_connections: int
