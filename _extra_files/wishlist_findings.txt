FES["Zustand stores (cart/currency/wishlist/...)"]
R["Domain routers: customer_coupons, customer_wishlist, admin_promotions ..."]
ROUTES["Route groups: admin · auth · cart · checkout · products · orders · supplier · logistics-partner · wishlist · profile · chatbot · tracking"]
CART["cartStore · wishlistStore"]
| 🟡 | D3 | backend | `models.products, schemas:185` | class name 'WishlistItem' is defined in 2 modules | rename or consolidate; duplicate class names create import/confusion drift |
| 🟡 | SYM2 | backend | `schemas:185, models.products:132` | class 'WishlistItem' defined in 2 modules | consolidate into one canonical definition |
- 🟡 **D3** `models.products, schemas:185` — class name 'WishlistItem' is defined in 2 modules → *rename or consolidate; duplicate class names create import/confusion drift*
- 🟡 **SYM2** `schemas:185, models.products:132` — class 'WishlistItem' defined in 2 modules → *consolidate into one canonical definition*
- 🟡 **DBA08** `backend\models\products.py:136` — FK column 'wishlist_items.user_id' has no explicit index signal → *every FK should have an explicit index*
- 🟡 **DBA07** `backend\models\products.py:136` — FK 'wishlist_items.user_id' missing ON DELETE rule → *default RESTRICT; CASCADE only for composition*
- 🟡 **DBA08** `backend\models\products.py:137` — FK column 'wishlist_items.product_id' has no explicit index signal → *every FK should have an explicit index*
- 🟡 **DBA07** `backend\models\products.py:137` — FK 'wishlist_items.product_id' missing ON DELETE rule → *default RESTRICT; CASCADE only for composition*
- 🟡 **DBA08** `backend\models\products.py:148` — FK column 'wishlists.user_id' has no explicit index signal → *every FK should have an explicit index*
- 🟡 **DBA07** `backend\models\products.py:148` — FK 'wishlists.user_id' missing ON DELETE rule → *default RESTRICT; CASCADE only for composition*
- 🟡 **DBA08** `backend\models\products.py:149` — FK column 'wishlists.product_id' has no explicit index signal → *every FK should have an explicit index*
- 🟡 **DBA07** `backend\models\products.py:149` — FK 'wishlists.product_id' missing ON DELETE rule → *default RESTRICT; CASCADE only for composition*
- 🟡 **DBA03** `backend\models\products.py:132` — model 'WishlistItem' table 'wishlist_items' missing: uuid, created_at/updated_at, soft-delete, version, created_by/updated_by → *use AuditMixin + SoftDeleteMixin + TenantMixin*
- 🟡 **DBA03** `backend\models\products.py:144` — model 'Wishlist' table 'wishlists' missing: uuid, created_at/updated_at, soft-delete, version, created_by/updated_by → *use AuditMixin + SoftDeleteMixin + TenantMixin*
- 🟡 **DBA03**:132 — model 'WishlistItem' table 'wishlist_items' missing: uuid, created_at/updated_at, soft-delete, version, created_by/updated_by → *use AuditMixin + SoftDeleteMixin + TenantMixin*
- 🟡 **DBA03**:144 — model 'Wishlist' table 'wishlists' missing: uuid, created_at/updated_at, soft-delete, version, created_by/updated_by → *use AuditMixin + SoftDeleteMixin + TenantMixin*
- 🟡 **DBA08**:136 — FK column 'wishlist_items.user_id' has no explicit index signal → *every FK should have an explicit index*
- 🟡 **DBA07**:136 — FK 'wishlist_items.user_id' missing ON DELETE rule → *default RESTRICT; CASCADE only for composition*
- 🟡 **DBA08**:137 — FK column 'wishlist_items.product_id' has no explicit index signal → *every FK should have an explicit index*
- 🟡 **DBA07**:137 — FK 'wishlist_items.product_id' missing ON DELETE rule → *default RESTRICT; CASCADE only for composition*
- 🟡 **DBA08**:148 — FK column 'wishlists.user_id' has no explicit index signal → *every FK should have an explicit index*
- 🟡 **DBA07**:148 — FK 'wishlists.user_id' missing ON DELETE rule → *default RESTRICT; CASCADE only for composition*
- 🟡 **DBA08**:149 — FK column 'wishlists.product_id' has no explicit index signal → *every FK should have an explicit index*
- 🟡 **DBA07**:149 — FK 'wishlists.product_id' missing ON DELETE rule → *default RESTRICT; CASCADE only for composition*

