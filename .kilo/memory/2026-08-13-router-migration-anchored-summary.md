# Project Anchored Summary (2026-08-14, updated)

## Goal
- **Workstream A (DONE):** Bulk-migrate legacy `routers/*.py` through the `routers -> controllers -> services` pipeline; keep layering gates green and `auto_router --verify` in sync.
- **Workstream B (ACTIVE):** Analyze the ~542 service files, then execute safe consolidation tiers (delete junk/scratch, dedupe exact duplicates, relocate misplaced real files) while keeping the audit and `auto_router --verify` green.

## Constraints & Preferences
- Layering rule: business logic in `services`/`providers`; controllers carry routing metadata only (import `get/post/...` from `routers.generated.auto_router`, never `APIRouter`); routers thin; **services must NOT import `APIRouter`**.
- PowerShell + `venv\Scripts\python.exe`. Never write controllers via `Set-Content -Encoding utf8` (adds BOM in PS 5.1); use the migrator or `open(path,'w',encoding='utf-8')`.
- Do NOT manually edit `ARCHITECTURE_MIGRATION_REPORT.md` (regenerated by `audit_migration_tracker.py`).
- `auto_router.py --verify` must stay green (148 modules / ~1770 routes). Do NOT decorate the ~186 adapter/delegator modules.
- **Avoid editing backend modules during the active concurrent consolidation** (another process is modifying `controllers/delegators/` and service files per git status).

## Progress

### Workstream A — Router Migration (DONE)
- Generator `routers/generated/auto_router.py` is source of truth: **33 modules / 224 routes, `--verify` OK, 15/15 gate+auto-router tests pass**.
- `tools/migrate_router.py` hardened (annotation-only type tokens, drop `APIRouter` from service import, abort if any route skipped, AST-validate before write).
- BOM issues fixed (`services/comms/whatsapp_service.py`, transient BOM on `export_controller.py`).
- Recovered wrong turn: re-migration overwrote clean HEAD export files from a `.pyc`; fixed via `git checkout HEAD --`.

### Workstream B — Service Consolidation
- **Audit fixed** (UTF-8 stdout, business-logic detector, adapter classifier): Controllers WITHOUT decorators 125 → 0; 148 decorated / 186 adapters / 0 genuine (at that time).
- **Tier 0 (DONE):** deleted 3 unreferenced scratch files — `services/common/run_py.py`, `script1.py`, `write_files_script.py`.
- **Tier 1 (DONE):** deleted `services/public/public_finance_creation_service.py` (byte-identical dup of `services/admin/admin_finance_creation_service.py`; 0 references). Kept admin copy.
- **Tier 2 (DONE — 2026-08-14):** relocated the two real files wrongly placed in singular/plural stray dirs:
  - `services/suppliers/admin_supplier_review_service.py` → `services/supplier/admin_supplier_review_service.py`
  - `services/communication/package_service.py` → `services/comms/package_service.py`
  - Updated 2 import sites (both are adapter delegators, excluded from decorator count): `controllers/delegators/suppliers.py:3` and `controllers/delegators/communication_package_service.py:3`.
  - Removed the now-empty `services/suppliers/` and `services/communication/` dirs.
  - **Verified:** `py_compile` exit 0 on all 4 touched files; audit `EXIT=0`, 148 decorated; `auto_router --verify` still **148 modules** (route count 1770→1765 from external router deletions, not this move). The only DRIFT (`admin_security_detection.py`, `public_security_detection.py`) is identical to pre-Tier-2 = pre-existing, unrelated.
  - Note: the "1 genuine missing decorator" and "25 missing items" (all `__init__.py` gaps in untracked `controllers/identity/`, `controllers/logistics/`, `models/supplier/`) are external-consolidation artifacts; the report contains zero mention of `suppliers`/`communication`, and Tier 2 only touched service files + adapter delegators.

## Blocked
- Deleting the 4 "empty" stub service files (`core/chatbot_service.py`, `logistics/logistics_service.py`, `orders/cart_controller_service.py`, `supplier/cash_management_controller_service.py`): BLOCKED — grep proved they are imported by live controllers/services. Unsafe to delete.
- Tier 3 (read/write fold, 88 files), Tier 4 (73 thin-forwarding), Tier 5 (37 structural near-dup pairs): blocked by active concurrent consolidation + import references.
- Clearing `auto_router --verify` DRIFT on security-detection routers: pre-existing from concurrent work; did NOT regenerate routers to avoid editing backend mid-consolidation.

## Key Decisions
- Tier 0/1 executed only because files were 0-reference (grep-verified) — zero behavior change.
- Tier 2 was assumed a delete but the dirs held real referenced code (262 + 170 LOC); became a MOVE, not a delete.
- The "1 missing decorator" and DRIFT are from the external consolidation, not our Tiers — confirmed by `py_compile` clean + audit/report showing no reference to the moved paths.
- 542 services largely legitimate (316 are 100+ LOC); inflation from ~3 scratch files (removed), 1 exact dup (removed), 4 imported empty stubs, and org sprawl.

## Next Steps
- After external consolidation settles: re-run `auto_router.py` to clear the security-detection DRIFT (or confirm it resolves on its own).
- Revisit Tier 3/4/5 (read/write fold, thin-forwarding, structural dups) once concurrent work done.

## Critical Context
- Service raw `.py` count (excl `__init__`): 542 → 539 after Tier 0+Tier 1; Tier 2 is a relocation (count unchanged at 539).
- Audit metrics now: Total Controllers 335 (swung via external process), 148 decorated, 1 genuine missing (external), 25 missing items (external `__init__` gaps).
- `auto_router --verify`: **148 modules**; DRIFT only on `admin_security_detection.py` + `public_security_detection.py` (pre-existing).
- `git status` shows active external edits: 30+ controller files modified, many routers deleted (D), new controller/service files untracked, `suppliers/admin_supplier_review_service.py` was M before Tier 2 (now moved to `supplier/`).
- Audit runs from repo root `audit_migration_tracker.py`; UTF-8 via `$env:PYTHONIOENCODING="utf-8"`.

## Relevant Files
- `audit_migration_tracker.py` — service/controller audit generator (v9); fixed earlier; re-runs confirm green.
- `ARCHITECTURE_MIGRATION_REPORT.md` — GENERATED; do not edit manually.
- `backend/services/supplier/admin_supplier_review_service.py` — MOVED here from `services/suppliers/` (Tier 2).
- `backend/services/comms/package_service.py` — MOVED here from `services/communication/` (Tier 2).
- `backend/controllers/delegators/suppliers.py` — import updated to `services.supplier` (Tier 2).
- `backend/controllers/delegators/communication_package_service.py` — import updated to `services.comms` (Tier 2).
- `backend/services/common/{run_py,script1,write_files_script}.py` — DELETED (Tier 0).
- `backend/services/public/public_finance_creation_service.py` — DELETED (Tier 1, dup of admin copy).
- `backend/services/admin/admin_finance_creation_service.py` — KEPT (canonical).
- `backend/tools/migrate_router.py`, `backend/routers/generated/auto_router.py` — Workstream A generator/migrator.
- `backend/services/core/logistics_service.py`, `orders/logistics_service.py`, `cart_controller_service.py`, `supplier/cash_management_controller_service.py` — empty stubs but IMPORTED; deletion blocked.
- `C:\Users\user\AppData\Local\Temp\kilo\analyze_services.py` — service analyzer (exact hash, rw pairs, thin-forwarding, similarity).
