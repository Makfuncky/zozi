// Tab-route proxy for /wishlist. Lets the wishlist page appear as a tab in
// the bottom bar (F-7 parity fix). Keeps the actual implementation in
// app/wishlist.tsx.
export { default } from "../wishlist";
